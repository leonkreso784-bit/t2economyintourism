# RASPORED — sve što čeka, razrezano na faze kroz sesije

**Status:** 🟩 AKTIVAN · **Otvoren:** 2026-09-04 (Leon: *„napravi strukturiran plan koji će ići
fazama kroz sesije da ga imamo da riješimo ovu listu kako treba"*)

> **Što ovaj dokument JEST:** **redoslijed i izlazni uvjet.** Odgovara na dva pitanja — *što sada*
> i *kad je faza gotova*.
>
> **Što NIJE:** nije drugi dom za mjerenja, nalaze i obrazloženja. Ta žive u
> [BACKLOG.md](../records/BACKLOG.md) i u zaglavljima skripti (ADR-027: jedna činjenica = jedno
> mjesto). Ovdje su **pointeri**, ne kopije — brojka prepisana ovamo ostarila bi istog dana.
>
> **Zamjenjuje:** `RACUN.md` kao tekući spec. R1 je isporučen i na produkciji; **R2 i R3 nastavljaju
> ovdje kao Faza 2** — spec je otišao u referencu i ondje objašnjava ZAŠTO je R1 izveden odjednom.

---

## 0 · SPAJANJE — što čeka na granama i kojim redom (sljedeća sesija = POPRAVCI)

> **Zašto ovo poglavlje postoji (2026-09-12).** Rad od 04.09. nastao je u **više radnih stabala
> istodobno** i živi na granama koje se međusobno NE sadrže: F1 je dovršen na jednoj osnovi, F2
> je počeo od `main`-a bez nje, kartice su parkirane na trećoj. Svaka grana je za sebe zelena, a
> zajedno ih nitko nije spojio — pa produkcija (zadnji 🚀 u [CHANGELOG.md](../records/CHANGELOG.md))
> ne nosi ništa od toga. Leon (12.09.): *„razdvojit sve planove i cigle na strukturni pametan plan
> pa ću krenuti na drugu sesiju sa popravcima."* Ovo je taj plan. **Koje grane postoje zna
> `git branch --no-merged main`, CI zna GitHub** — ovdje je samo ŠTO nose i KOJIM REDOM idu.

| grana | nosi | odrezana od | prije spajanja |
|---|---|---|---|
| **`fix/napredak-pouzdanost`** | BUG-047…051 (napredak pouzdan: identitet kartice · merge · snapshot · vlasnik · loader) + `check:docs` popravak (cherry-pick `3bfe40a`) | `main` | ništa — CI zelen, preflight 0, suita zelena |
| **`feat/mjerenje-aktivacije`** | ostatak **F1**: F1/4 · F1/5 · F1/7 · F1/9 · F1/11 · **metrika** `ucenje`/`povratak` (consent-gated) · docs 05.–12.09. (ADR-035/036, matura u archive/, memorija-čišćenje). **Sadrži cijelu `feat/racun-r1`** (samo docs) | `main` | sudari su **tekstualni**: CLAUDE/RASPORED/PROGRESS/CHANGELOG/BUGS + `?v=` tokeni (`npm run bump` iznova). `flashcards.js`: F1/9 dodaje gestu, BUG-047 mijenja samo `saveFlashcardProgress` — različita mjesta |
| **`feat/nocna-b`** | **F2/1 (6/7)** — birač tema: natpis samo „Automatski" · popravak `check:docs` (isti kao gore) | F1-osnova (`900f142`) | **nije pushana** → nema CI-ja; push prvo. ⚠️ Sadržana je i u `feat/tinder-kadar` — spaja se **ova**, ne tinder |
| **`feat/profil-zid`** (⊃ `feat/traka-odredista`) | **F2/0** traka s dva odredišta · **F2/3a** profil = zid · **F2/3b** identitet (`profile_identity` + `set_profile_identity` RPC, `supabase/f2-profile-identity.sql`) | `main` (bez F1-osnove!) | ⚠️ **migracija je SAMO na stagingu** → na PROD uz izričit OK i PRIJE deploya, inače profil puca. Docs-sudar s F1-granom: svježija strana nosi istinu (F1 = mjerenje-aktivacije, F2 = ova) |
| `feat/tinder-kadar` (⊃ `feat/nocna-b`, ⊃ `fix/kadar-nalicje`) | kartice F1/12–13 — **PARKIRANO** (Leon 08.09.; zapis `docs/archive/KARTICE_TELEFON.md` postoji **samo na toj grani**) · **BUG-045** (`3708e2d`, SW runtime-keš) · **BUG-046** (`2641834`, kontrast pilule — kvar JE na produkciji) | F1-osnova | **NE spaja se** (CI crven, nije flake). BUG-045/046 idu **cherry-pickom**, nikad vrh grane |
| `feat/racun-r1` | samo docs, sve već u `feat/mjerenje-aktivacije` | F1-osnova | obrisati nakon što se ona spoji |

**Redoslijed — prijedlog, svaki korak je svoja cigla:** preflight + `test:responsive` zeleni → **Leonov OK za `main`** → deploy je **zaseban** OK (§1). Nijedan korak se ne spaja „po sjećanju": sudar u docs rješava se tako da **svježija grana pobijedi**, a `check:state`/`check:docs` presuđuju.

1. **`fix/napredak-pouzdanost` → `main`.** Najmanja, neovisna, sama vrijedi korisniku (broj „naučeno" i sync su od danas istiniti).
2. **`feat/mjerenje-aktivacije` → `main`.** Donosi ostatak F1 i **istinu docs-a** (ovaj RASPORED dolje je stanje s `main`-a — F1 tablica ondje je svježija i pregazit će ovu). Zatim obrisati `feat/racun-r1`.
3. **`feat/nocna-b` → `main`** (F2/1 6/7). Prvo push, pa CI.
4. **`feat/profil-zid` → `main`** + migracija `f2-profile-identity.sql` na PROD (izričit OK) → deploy. Zatim obrisati `feat/traka-odredista`.
5. **Cherry-pick `2641834` (BUG-046) i `3708e2d` (BUG-045)** s parkirane grane — svaki svoj commit, svaki svoj gate.
6. Tek tada **F2/2 slike** u `.f2` — na `main`-u koji je opet **jedan**. Novo stablo = nova sesija (higijena: jedna sesija po stablu, nova po fazi).

**✅ Izvedeno 13.09.:** svih pet koraka je na `main`-u i na produkciji (pet pushova, svaki uz Leonov OK i preflight u
pre-push kuki; migracija koraka 4 primijenjena na PROD prije njegova deploya). Zapis: 🚀 13.09. u
[CHANGELOG.md](../records/CHANGELOG.md) · brojke suita u [PROGRESS.md](../records/PROGRESS.md). Spojene grane i
`spajanje/*` obrisani. **Ovo poglavlje je time ispunjeno** — ostaje kao zapis zašto je spajanje išlo ovim redom.
Korak 6 = F2/2 u novoj sesiji, u novom stablu od `main`-a.

**Što se u toj sesiji NE radi:** ne otvaraju se kartice (parkirano), ne dira se gradivo, ne uvodi se ništa novo — samo se ono što JEST zeleno dovodi na jedno mjesto, korak po korak, s Leonovim OK-om na svakom pushu.

---

## 1 · Kako se ovaj raspored vozi

Pravila su Leonova i ne mijenjaju se između faza:

| pravilo | što znači u praksi |
|---|---|
| **Jedna cigla = jedan commit** | Zahvat koji dira zajednički šav se **reže**, ne dovršava u komadu. Signal: ako popravak traži više od par datoteka koje nisu predmet cigle — to više nije jedna cigla, nego serija. |
| **Zastanak na kraju faze** | Unutar faze teče cigla za ciglom (gate na svakoj). Na kraju faze **stani i javi se.** |
| **Deploy = uvijek zaseban izričit OK** | Nijedno ranije odobrenje se ne proteže na sljedeći put. |
| **Uz svako „popravljeno" reci GDJE se vidi** | produkcija / preview / samo lokalno. Bez toga Leon testira stari kod. |
| **Mjeri prije nego popravljaš** | Protučinjenični pokus, pa hipoteza. Alat bez kontrole nije mjera. |
| **Dok Playwright vrti — ne diraj datoteke** | Suita traje ~33 min i čita s diska; izmjena usred vrtnje čini rezultat bezvrijednim. |

**Redoslijed faza nije proizvoljan.** Prve dvije su ono što Leon osjeti na svom uređaju; treća
otvara stranicu drugoj polovici publike; četvrta plaća dug koji koči svaku sljedeću vizualnu ciglu;
peta, šesta i sedma su rast proizvoda. Faza se smije preskočiti Leonovom riječju — ali onda se
preskače svjesno, a ne zaboravom.

---

## 2 · Faze — pregled

| # | faza | zašto baš tu | gotovo kad korisnik… |
|---|---|---|---|
| **F1** | **UREĐAJ** — izgled i glatkoća | Leon to vidi na svom telefonu svaki dan; sve je ili izmjereno ili traži jedno mjerenje | …otvori stranicu na telefonu i zatekne **isto što i u mailu** — crno na tamnom uređaju, svijetlo na svijetlom — bez bljeska, bez trzanja pri skrolanju i bez gumba koji svijetle sami od sebe |
| **F2** | **RAČUN** — profil i obavijesti | nastavak tekućeg bloka; profil je ujedno i najveći komad CSS-duga, pa ide u istom prolazu | …se prijavi s bilo kojeg uređaja i zatekne svoju temu, svoju sliku i svoje ime — i primi mail samo ako je na njega pristao |
| **F3** | **DVOJEZIČNOST** — 421 → 0 | Leonova presuda (ADR-033); ~~četiri stranice nemaju ni mehanizam~~ — **F3/1 ✅ 16.09. na grani** (ADR-037) | …prebaci jezik i **nigdje** ne naiđe na engleski ostatak, uključujući pravne stranice i editor |
| **F4** | **ČIŠĆENJE** — CSS dug do nule | ne blokira ništa vidljivo, ali svaka sljedeća vizualna cigla plaća kamatu na njega | …ne primijeti ništa — ovo je faza u kojoj se ne smije promijeniti nijedan piksel |
| **F5** | **VJEŽBE** — recepti pa izgled | jedina velika površina bez ijednog vizualnog gatea; recepti su preduvjet za sve što dolazi poslije | …rješava vježbu koja izgleda kao ostatak platforme, i (nevidljivo za njega) vježba je postala podatak kao i sve drugo |
| **F6** | **MCP** — cjevovod stvaranja | glavni put nastanka gradiva (ADR-030/031); konektor traži OAuth, koji F2 dovršava | …svom AI-ju da materijal kroz chat i dobije nacrt lekcije, kartica i kviza — a da mi tu datoteku nikad ne vidimo |
| **F7** | **OBJAVA** — dijeljenje i povijest | dijeljenje daje smisao povijesti učenja i grafikonima napretka, pa idu zajedno | …podijeli svoj materijal linkom i vidi kad je zadnji put što učio |

> **⚠️ IZVAN FAZA — MJERENJE AKTIVACIJE I POVRATKA (07.09.).** Preskočilo je red jer ima **datum, a ne
> mjesto u fazi**: val korisnika dolazi s početkom fakulteta, a do danas se mjerio samo **dolazak**
> (GA4 šalje pregled stranice i tu prestaje; Vercel Web Analytics je ugašen). Bez ovoga listopad
> prođe bez traga — ne bi se znalo je li itko od tih posjetitelja **počeo učiti**, ni je li se
> **vratio drugi dan**, a to su jedine dvije brojke koje razlikuju posjet od proizvoda.
> **Gotovo kad** se u GA4 vidi koliko je posjetitelja otvorilo način učenja i koliki im je redni
> različit dan. Isporučeno kao jedna cigla: `window.SokratMetrika` u `js/consent.js` (isti gate
> pristanka kao GA i Sentry) + jedna kuka u `switchSection()`.

> **⚠️ PRIORITET — MCP „VRLO USKORO" (Leon, 16.09.).** *„prije nego što napravimo cijelu mrežu ja bih
> se krenuo baviti MCP-om i to VRLO uskoro, jer smo polako na kraju s vremenom prije početka faksa."*
> **✅ Red presudio Leon (anketa 16.09., poslije F3/2-3):** F3/2 **profil + Moji materijali + prijava**
> (stranice na koje stiže ono što MCP napravi) → **F6 MCP** → ostatak F3/2 (vježbe, sitnice) · F3/3 editor ·
> F4 · F5 · F7. Činjenica koja ide uz F6/1: konektor traži OAuth nad računom iz F2.

---

## 3 · Faze u detalje

### F1 · UREĐAJ

> ### 🔄 FAZA JE PRERASLA SVOJ OPIS (2026-09-04)
> F1/1 je poravnala **svijetlu** temu s mailom — i to stoji. Ali mail ima **dvije palete i mijenja
> se s uređajem**, a ja sam izmjerio samo jednu granu medija-upita. Leonov telefon je u tamnom, pa
> je njemu mail oduvijek crn. **Njegova odluka: stranica prati uređaj, točno kao predložak.**
> Time F1 dobiva tri nove cigle, a stara F1/2 postaje njihova posljedica, ne susjed.
> Mjere, heksovi i zamke: [BACKLOG.md](../records/BACKLOG.md) §CRNA TEMA.

**Redoslijed prvenstva teme — zapisan JEDNOM, jer se inače svaki put izvodi iznova:**

> **račun (F2/1)  >  lokalni izbor u profilu  >  uređaj (`prefers-color-scheme`)  >  `academic`**

| cigla | posao | gotovo kad |
|---|---|---|
| **F1/1** ✅ | ~~**Svijetla tema = svijetla polovica maila.**~~ **Isporučeno 2026-09-04** — `brand-400 #6366f1` (plohe/hover/fokus) · `brand-500 #4f46e5` (tekst i gumbi) · `brand-600 #4338ca` + swatch; `og-cover.png` pregrađen. | …stranica i mail izgledaju kao ista marka; `check:contrast` 292/292 i `check:contrast:live` **0 ispod praga** |
| **F1/2** ✅ | ~~**Crna tema = tamna polovica maila.**~~ **Isporučeno 2026-09-04** — tema `carbon` („Ugljen"), doslovni heksovi predloška, indigo obrnut (500 `#818cf8`, on-brand taman). Živa brana je usput razotkrila da se `brand-600` na ~8 mjesta koristi kao ispuna **u mirovanju** (protiv ugovora tokena) — carbonov 600 zato `#7075f4`, a CSS-zloporaba ide u **F4**. [BACKLOG.md](../records/BACKLOG.md) §CRNA TEMA. | …korisnik ju odabere u profilu i cijela stranica izgleda kao njegov mail; `check:contrast` 358/358, `check:contrast:live` **0 ispod praga** na 4 teme |
| **F1/3** ✅ | ~~**Prvi kadar prati uređaj.**~~ **Isporučeno 2026-09-05** — `boot.js` bez spremljenog izbora čita `prefers-color-scheme` prije prvog crtanja (tamno → `carbon`, svijetlo → `academic`); birač dobio **„Automatski · ‹što uređaj bira›"** kao prvu opciju, aktivan je IZBOR a ne primijenjena tema; `theme.js` prati promjenu uređaja uživo dok je izbor automatski. ⚠️ **Nalaz izvan speca:** produkcija je na SVAKOM učitavanju upisivala `academic` u `sokrat-theme`, pa bez migracije crno ne bi dobio **nitko** tko je stranicu već otvorio — izbor od sada nosi biljeg `sokrat-theme-chosen`; `academic` bez biljega = nije izbor, `chalk`/`mint` bez biljega = jest. Dokaz: `tests/unit/theme-device.test.js` (28 tvrdnji u `vm` sandboxu) · `theme-fouc.spec.js` +4 (emulirani uređaj, stari zapis, izbor pobjeđuje) · `fouc-probe` scenarij `uredjaj-tamni` = **0 ms bljeska** u oba profila. | …Leon otvori stranicu na svom telefonu i **odmah** vidi crno, bez ijednog klika i bez bljeska — ⚠️ ako na tom telefonu nije ranije **birao** Ploču/Mentu (izbor pobjeđuje uređaj; „Automatski" u profilu ga vraća) |
| **F1/4** ✅ | ~~**Zatvori rupu u brani.**~~ **Isporučeno 2026-09-06:** popis tema ima JEDNO mjesto — `scripts/teme.js` čita `:root[data-theme="…"]` blokove iz `tokens.css` (komentari se skidaju; **nula tema = greška, ne prazan niz** — brana koja prođe nula tema i šuti je točno kvar koji se zatvara), a kroz njega idu SVA četiri čitatelja: `check-contrast-live.js` (bio zakucan), `tests/helpers/axe-gate.js` (bio zakucan s mrtvim `paper`, bez `carbon`), `check-contrast.js` i `theme-boot-order.test.js` (imali vlastiti regex). `boot.js` zadržava svoj niz (vrti se prije CSS-a) i test ga čuva protiv modula. Brana `tests/unit/theme-list.test.js` (24 tvrdnje; obrnuto kroz `git worktree` na starom stablu = **9 crvenih**). **Dokaz:** prijavljena a11y-suita **prvi put skenira `carbon`** na 7 ploha — 0 nalaza; `check:contrast:live` 13 ruta × 4 teme *iz tokena*, 0 ispod praga; preflight EXIT 0. Plan je bio: `scripts/check-contrast-live.js:39` ima **zakucan popis tema** — nova tema bez toga prolazi neizmjerena, a brana ostaje zelena. Popis mora doći iz `tokens.css`, kao što ga staticka brana već čita. ⚠️ **Isti razred, nađen u F1/11:** `tests/helpers/axe-gate.js` `TEME` je isto zakucan — nosi `paper` (maknut 2026-09-01) i **nema `carbon`**, pa a11y-suita carbon ne skenira, a `paper` skenira kao zadanu. | …živa brana mjeri svaku temu koja postoji, a ne onu koju je netko upisao rukom |
| **F1/5** ✅ | ~~**Četiri stranice bez teme**~~ **Isporučeno 2026-09-06:** `contact` · `faq` · `privacy` · `terms` dobili `<html data-theme="academic">` + **sinkroni `boot.js`** na vrhu `<body>` (isti mehanizam kao `index.html`, nijedna nova kopija — `tokens.static.css` je sve teme već nosio, `legal.css` je od MREŽE bez ijednog heksa). Brane: `theme-boot-order.test.js` sad traži boot na SVAKOJ stranici, ne samo onima s biračem (obrnuto kroz `git worktree`: 8 crvenih na starom stablu) · `tests/legal.spec.js` +2 mjere ISCRTANO — `prefers-color-scheme: dark` → `carbon`, `light` → `academic`, `color-scheme` na `<html>`, pozadina `<body>` == `--color-surface-0` teme (ne kopija heksa) · `check:contrast:live` dobio 4 pravne rute (4 × 4 teme, 0 ispod praga). Preflight EXIT 0, bump. **Svjesno NE:** `viewport-fit=cover` (legal.css i dalje bez safe-area; sprega u `touch-zoom.test.js` stoji) · **prijevod F3/1 nije uzet** — to je F3, Leonova riječ. **Leon isti dan, gleda produkciju:** *„frontend nije prilagođen, teme ne odgovaraju, taj dio je uvijek bijela tema"* — točno ovo, i točno stanje `c53c28c` (F1/5 je na grani). Plan je bio: `contact` · `faq` · `privacy` · `terms` nemaju ni `data-theme` ni `boot.js`. ⚠️ **Od F1/3 ovo prestaje biti sitnica:** čim stranica prati uređaj, korisnik na tamnom telefonu dobiva crn katalog i **bijela Pravila privatnosti**. Isplati se uzeti i njihov prijevod (F3/1) u istom obilasku. | …korisnik na tamnom uređaju otvori Pravila privatnosti i ne zaboli ga oko |
| **F1/6** ✅ | ~~**Sonda za trzanje pri skrolanju.**~~ **Isporučeno 2026-09-05** — `scripts/jank-probe.js` (mjerni instrument izvan preflighta, kao `fouc-probe`): mobilni profil 393×852 @3x, CPU 4×, **prst** (ručni niz touch-događaja — sintetička CDP gesta u headlessu ne miče stranicu, izmjereno), 3+3 zamaha po ruti; mjeri kadrove kroz rAF, LoAF, `Paint` + prebojanu površinu i `DroppedFrame` iz tracea, layout/recalc/skriptu s glavne niti, i **ispisuje doseg** (ruta × scenarija, gesti, kadrova). **Nalaz (10 ruta, 5 363 kadra):** glavna nit je čista svugdje (median 16,7 ms, najviše 3 kadra od ~540 na 33 ms, 0 layouta tijekom skrola); **jedina ruta koja preboji je landing** — 240 paintova, **533 Mpx** (≈ 70 % ekrana SVAKI kadar), 28–54 ispuštena kadra, dok svih devet ostalih ima **0 paintova i 0 ispuštenih**. **Protučinjenično na landingu** (6 scenarija): bez zamućenja / bez sjena / bez prijelaza = **isto** (240 paintova); **bez `background-attachment: fixed` = 0 paintova, 0 ispuštenih** — jedini izmjereni uzrok. ⚠️ **Instrument je Chromium**: iOS Safari `background-attachment: fixed` ionako crta kao `scroll`, pa je ovo dokaz troška na Androidu i stolnom Chromeu, a Leonov iPhone može trzati iz razloga koji ovaj alat ne vidi (ondje najsumnjivije: `backdrop-filter` na ljepljivoj traci) — zato F1/7 nosi A/B na pravom telefonu. Brojke: [BACKLOG.md](../records/BACKLOG.md) §LEONOVI NALAZI B. | …postoji brojka koja kaže gdje se gube kadrovi — bez nje se ne dira ništa |
| **F1/7** ✅ | ~~**Popravak po nalazu sonde.**~~ **Isporučeno 2026-09-05** u dva commita. **① landing** (`9139d6f`): `background-attachment: fixed, fixed, …` → **`scroll`** u `css/landing.css` — odsjaj putuje s herojem; iOS Safari je `fixed` ionako crtao kao `scroll`, pa iPhone ne mijenja izgled. **Mjera istog dana** (`jank-probe`, landing, kontrola): PRIJE paint **240 / 532,7 Mpx**, ispušteno **94**; POSLIJE paint **0 / 0**, ispušteno **0**. Brana `tests/unit/no-fixed-background.test.js` (bundle + izvor bez komentara; landing zadržava zrno i odsjaj — popravak je `scroll`, ne brisanje; obrnuto: 2 crvene). **② prekidač `?bez=`** za iPhone: `css/bez.css` (novi modul, zadnji u manifestu; `!important` NAMJERAN i imenovan za F4/3) + `js/boot.js` čita `?bez=zamucenja,sjena,prijelaza,pozadine` i PRIJE prvog crtanja upiše `data-bez` na `<html>` (sanirano na `[a-z-]`, nadživi `replaceState`); **sonda više ne nosi vlastitu kopiju zabrana** — popis scenarija čita iz `bez.css` i postavlja isti atribut (ADR-027; `bez-fiksne-pozadine` otpao s ①). Dokazi: `bez-switch.test.js` (26 tvrdnji, obrnuto na HEAD-u 5 crvenih; komentar modula s primjerom `ime` bio je pokupljen kao scenarij → skidanje komentara u testu I sondi) · živa sonda u Chromiumu: zamućenje 5 → 0, sjene 27 → 0, prijelazi 187 → 0, pozadina → `none`, kontrola netaknuta, nepoznato ime inertno · `jank-probe --rute=landing` 5/5 scenarija kroz atribut, paint 0/0 u svima · preflight EXIT 0. ⚠️ **Otvoreno = Leonov A/B na iPhoneu** (preview): `?bez=zamucenja` · `?bez=sjena` · `?bez=prijelaza` · `?bez=pozadine`, pa kombinacija — koji je gladak, taj postaje popravak. Hipoteza je bila, napisana TEK iz F1/6: ① **landing** — dva sloja s `background-attachment: fixed` (zrno + odsjaj) prebojavaju ekran svaki kadar → ili `scroll` (odsjaj putuje s herojem, gdje mu i jest mjesto) ili isti sloj kao `position: fixed` pseudo-element iza sadržaja (kompozitor ga nosi bez paintova); dokaz = `jank-probe` vraća **0 paintova** na landingu. ② **iPhone** — instrument ga ne vidi, pa preview dobiva prekidač `?bez=zamucenja|sjena|prijelaza` (iste `!important` zabrane koje sonda ubrizgava) i Leon na svom telefonu kaže koji je scenarij gladak; tek iz toga popravak. Nijedan „na oko". | …Leon skrola po svom telefonu i kaže da je glatko (on presuđuje, ne brojka) |
| **F1/8** ✅ | ~~**Ljepljivi hover nakon prelaska.**~~ **Isporučeno 2026-09-05** — **② miš:** `pauzirajHover()` (`js/utils.js`) iz `navigateTo` I `browseNaRazinu` (browse-prelazi ne idu kroz `navigateTo` — plan je to previdio, sonda našla), prvi `pointermove` skida; prefiks `:where(:root:not([data-hover-paused]))` na **142/142** selektora u istom `hover-css.js` prolazu, `check:hover` ga traži (obrnuto 134 → 0); `hover-probe --profil=prelaz` **ljepljivo 2/2 → mirno 2/2 + naoružano 2/2**; miš `--usporedi` 0 razlika; css:diff 7 161 usporedbi 0 razlika; `hover-arm.test.js` 28 tvrdnji; BUG-044 zatvoren. **① dodir isporučen 2026-09-05:** `scripts/hover-css.js` u `build:css` (lightningcss **čita** stablo s precima, **tekst** bundlea se prepisuje po `loc` — vraćanje stabla u Rust puca i promijenilo bi svaki bajt) zamata **130 pravila / 142 selektora** u `@media (hover: hover)` na istom mjestu kaskade; liste cijepane (`:focus-visible` ostaje vani), `:hover` u `:not()/:is()` = pad builda; `legal/consent.css` ručno. Brana **`check:hover`** u preflightu (obrnuto: 130 golih na starom bundleu). `scripts/hover-probe.js`: WebKit dodir **ljepljivo 2/2 → mirno 2/2**, miš 38 elemenata **0 razlika** (20 i dalje mijenja izgled), css:diff 0 razlika (2 rute × 3 širine, 7 161 usporedbi). **Leon na iPhoneu (preview `92269c2`): *„Ne svijetli, odlično."*** Nalaz (Leon, 2026-09-05: *„gumb koji je stajao na mjestu starog gumba isto svijetli po rubovima a nije ga se diralo, to je jako naporno"* — *„imamo taj problem od početka"*). **Izmjereno 2026-09-05 na dva motora:** na mišu se reproducira 100 % u oba; na **dodiru ga reproducira WebKit** — motor SVAKOG preglednika na iPhoneu, pa pitanje „koji preglednik" otpada — a Chromium s dodirom ne. Nakon dodira koji mijenja rutu kartica pod točkom odmah nosi `:hover` (rub `brand-500`, −4 px, sjena) i ne prolazi ni nakon 3 s ni nakon idućeg dodira. Fokus nije uzrok: `activeElement` ostaje `BODY`, a 12 pravila golog `:focus` sva su na poljima za unos. **Protučinjenično potvrđeno:** pravilo pod `@media (hover: none)` vraća karticu u mirni izgled u WebKitu s dodirom (emulacija i pravi iOS javljaju `hover: none`), a na mišu ne mijenja ništa. Dodir šalje `mousemove`, ali **ne** `pointermove`; poslije prelaska bez pomaka nijedan motor ne šalje `pointermove` → siguran okidač za miš. CSS: **138 `:hover` pravila** (148 pojava) u 27 datoteka, **66 mijenja rub** (border/box-shadow/outline), 3 zaštićena. **Serija od DVIJE cigle** — treća („`:focus` → `:focus-visible`") otpada, nema što popraviti: ① **dodir** — svaki `:hover` selektor pod `@media (hover: hover)` kroz `build:css` (selektor-lista se **cijepa**: `:focus-visible` iz istog pravila ostaje vani); brana = statička provjera bundlea u preflightu + WebKit-sonda izvan njega · ② **miš** — hover se naoruža tek prvim `pointermove` poslije promjene rute (`data-hover-paused` na `<html>`, prefiks `:where(:root:not([data-hover-paused]))` = nula specifičnosti). Mjere i tablica: [BACKLOG.md](../records/BACKLOG.md) §LEONOVI NALAZI D. | …Leon pritisne gumb, dođe na drugu stranicu i **nijedan** gumb ondje ne svijetli dok ga ne dotakne |
| **F1/9** ✅ | ~~**Kartice kao Tinder-špil na telefonu**~~ **Isporučeno 2026-09-06** (Leon isti dan, usred rada: *„samo na mobitelu, ne vidim kako bi mogao funkcionirati na kompu … ali ako imaš viziju probaj nešto"*): **palac** — povlačenje desno = znam, lijevo = ne znam, dodir okreće, kratko povlačenje se vraća bez upisa, okomit pomak ostaje skrol; **špil** = dvije sjene ispod kartice u boji SVOJE kartice (`applyAccent`), samo pod `pointer: coarse` — stolno izgleda kao danas; **pečati** „Znam / Ne znam" rastu s prstom (puna ispuna ok/danger + tinta po temi, ADR-032; `visibility` u mirovanju); let van ekrana pa upis kroz **postojeći `markKnown`/`markUnknown`** — jedini put upisa, brana ga BROJI; `prefers-reduced-motion` = bez leta. **Stolni pandan (Leonovo „probaj nešto"):** strelice → / ← = znam / ne znam, razmak/Enter = okreni, uz isti let i pečat — samo u modu kartica, nikad iznad polja, gumba, poveznice ili otvorenog modala, nikad s modifikatorom. Bez biblioteke: pointer-događaji, JS piše tri CSS-varijable (`--swipe-x/rot/p`) i tri klase, crtanje je u CSS-u; `touch-action: pan-y` na kartici **i na licu/naličju** = jedino odstupanje od reseta F1/11 (štipanje ostaje ugašeno, `touch-zoom` brana broji). **Dva nalaza koje je dao samo PRAVI dodir (CDP-sonda), ne pješčanik:** ① `touch-action` se čita od dodirnutog elementa do PRVOG skrolera — lice i naličje su skroleri (`overflow-y: auto`, BUG-013), pa `pan-y` samo na `.flashcard` nikad ne dođe na red i gesta dobije `pointercancel` na prvom pomaku; ② Chromium poslije brzog vodoravnog zamaha POTISNE `click` sljedećeg dodira (izmjereno na goloj stranici, i 3 s kasnije) → okretanje na dodir ide na `pointerup`, ne na `click` (klik koji ipak stigne se guta). Brane: `tests/unit/flashcard-swipe.test.js` (78 tvrdnji: gesta u `vm`-pješčaniku, strelice, jedan put upisa, CSS/markup; obrnuto kroz `git worktree` = 48 crvenih) · `tests/flashcard-swipe.spec.js` (PRAVI dodir kroz CDP `Input.dispatchTouchEvent` na 4 iPhone profila: upis + brojke · kratko = povratak · pečat vidljiv samo dok gesta traje · špil 2/1/0 · reduced-motion · strelice · protučinjenično: reset samo na `.flashcard` = gesta radi, reset na licu/naličju = nema upisa). Preflight EXIT 0, bump. **Umjesto §6/6 — odluka:** gesta je DODATAK uz gumbe, ne zamjena → prekidač nije potreban; smjer nosi značenje. ⚠️ **Leon isti dan navečer, s previewom u ruci:** smjer = LISTANJE (lijevo prethodna, desno sljedeća), sud = gumbi ✓ / ✕ dolje kao na Tinderu, kartica premala → **F1/12 + F1/13** (izmjereno, plan; kod F1/9 ostaje kao mehanika). **Presuda palcem = Leonov iPhone** (headless nema Safarijev gesture-put). Plan je bio: jedna kartica, klik okreće, četiri gumba, nijedne dodirne geste; zadano — dodir okreće · desno = znam · lijevo = ne znam · špil od 3 · gumbi ostaju · stolno kao danas · bez biblioteke. | …Leon na telefonu prolazi špil palcem bez ijednog gumba, a „znam / ne znam" je isto stanje koje vide statistika i cloud-sync |
| **F1/10** ✅ | ~~**Zoom na dodir.**~~ **Isporučeno 2026-09-05** (Leon: *„kada se više puta takne na jedno mjesto može se zoomat, to se mora riješit"*). Dva uzroka, oba u resetu `css/variables.css`: ① **polja ≥ 16 px na dodiru** kroz `@media (pointer: coarse)` — iOS pri fokusu manjeg polja sam zumira. ⚠️ **Zatečeno:** isto je već „štitilo" pravilo iza `@supports (-webkit-touch-callout: none)` s `!important`, a taj upit **ne zadovoljava nijedan motor naših brana** (`CSS.supports` false u Chromiumu i Playwrightovom WebKitu) — nemjerljivo godinu dana; `(pointer: coarse)` je istina u oba (izmjereno) i na iPhoneu, specifičnost 0,2,1 umjesto sile. ② **`touch-action: manipulation` na `*`** (+ `legal.css`) gasi dvostruki dodir, skrol i štipanje ostaju; `user-scalable=no` odbačen — **obrnuto istog dana navečer: ADR-034 → F1/11**. **Brane:** tvrdnja **⑨** `phone-gate`-a u obje suite (sva polja, i skrivena — modal prijave) — obrnuto: stari bundle **11 × 13 × 4 crveno → 0**; `tests/unit/touch-zoom.test.js` (bundle + `legal.css`; obrnuto 6/9 crveno). Sonda u oba motora: točno 11 polja → 16 px, 52 netaknuta. **Usput:** `phone.authed.spec.js` mjerio je telefon bez `hasTouch` (miš) — ⑨ ga razotkrila, popravljen, 12/12. Dvostruki dodir **nemjerljiv u headlessu** → Leon na iPhoneu: **potvrdio 2026-09-05 navečer** (*„više ne, sređeno je"*). Zapis: BUG-043 · [BACKLOG.md](../records/BACKLOG.md) §LEONOVI NALAZI F. | …Leon dodirne polje ili dvaput dodirne tekst i stranica **ostane iste veličine** |
| **F1/11** ✅ | ~~**Bez zuma uopće — stranica ostaje na mjestu.**~~ **Isporučeno 2026-09-05** (Leon ujutro: *„Ovo što mi se dešava je svugdje, nije samo na Safariju, kreni."*) — **meta** `minimum-scale=1.0, maximum-scale=1.0, user-scalable=no` na svih 6 stranica; pravne **bez** `viewport-fit=cover` (`legal.css` nema nijednog safe-area razmaka, cover bi u polegnutom položaju gurnuo tekst pod izrez — sprega meta ⇔ safe-area je tvrdnja u testu) · **reset** `touch-action: manipulation` → **`pan-x pan-y`** u `variables.css` + `legal.css` (skrol ostaje, dvostruki dodir i štipanje otpadaju; iOS-sloj je bio JEDAN redak, ne novi mehanizam — `manipulation` dopušta pinch-zoom, `pan-x pan-y` ne) · **a11y:** `ISKLJUCENO_ODLUKOM` u `tests/helpers/axe-gate.js` — jedno imenovano isključenje s razlogom ADR-034, ne deset redaka osnovice (ona ključa po površini i očekuje da nalaz NESTANE); pravilo se i dalje vrti i ispisuje kao `[a11y-odluka] ISKLJUČENO`. **Obrnuto:** bez isključenja landing pada na `meta-viewport` (moderate/AA); `touch-zoom.test.js` prepisan (35 tvrdnji) i vrćen kroz `git worktree` na starom stablu = **13 crvenih**; `a11y-gate.test.js` +5 (po id-u, ne po razini). Preflight EXIT 0, a11y-suita 7/7. **Leon na iPhoneu (isti dan, kasno navečer):** dodir u polje i dvostruki dodir *„više ne, sređeno je, good job"* ✅ — ali *„sa dva prsta"* i dalje zumira: `pan-x pan-y` Safari za štipanje NE drži (izmjereno na uređaju, jedino mjesto gdje se može). → **② JS-sloj `js/no-zoom.js`** na svih 6 stranica (`defer`; vlastita datoteka, jer pravne nemaju `boot.js` — F1/5 time NIJE preduvjet): `gesturestart` / `gesturechange` + `touchmove` sa `scale !== 1` → `preventDefault()` uz `passive: false`, veže se SAMO gdje `GestureEvent` postoji (Chrome/Android slušaju metu, nepasivan `touchmove` bi im usporio skrol). Brana: `touch-zoom.test.js` ③ — 6 stranica × tag + sandbox (bez `GestureEvent` nula slušača; s njim tri, svi `passive: false`; `scale 2` otkazan, `scale 1` i bez `scale` ne). **Presuda štipanja opet = iPhone.** Plan je bio (Leon, 2026-09-05 poslije deploya `c53c28c`: *„Stranica uopće ne bi trebala imati mogućnost da se nešto povećava ili smanjuje na njoj ikako. Treba ostati na mjestu."* → **ADR-034**, obrće odbijanje `user-scalable=no` iz F1/10). Plan: `maximum-scale=1, user-scalable=no` na svih **6** stranica (4 pravne danas nemaju ni `viewport-fit`) **+ iOS-sloj**, jer Safari od iOS-a 10 **ignorira** `user-scalable=no` za štipanje — kandidati `touch-action: pan-x pan-y` na korijenu i/ili `gesturestart` → `preventDefault()`; što drži, **zna samo iPhone** (headless ne izvodi štipanje, BUG-043). a11y-gate: axe `meta-viewport` je AA → **imenovana iznimka s razlogom ADR-034**, ne šutnja. Tvrdnja ⑨ ostaje (fokus-zoom meta na iOS-u ne gasi). Brana: statički test da svih 6 meta nosi oba atributa i da iOS-sloj postoji (obrnuto: danas crveno); dokaz = Leon na iPhoneu. | …Leon na iPhoneu štipa, dvaput takne i takne polje — stranica se ne poveća, ne smanji i ne pomakne |
| **F1/12 ⓪** | **Platforma ZNA uređaj — jedno mjesto** (Leon, 2026-09-06: *„Platforma mora znati na kakvom je uređaju korisnik."*). Danas se to pita na više mjesta i na više načina: CSS `@media (pointer: coarse)` (F1/10, F1/9), `(hover: hover)` (F1/8), JS `GestureEvent` (no-zoom.js), `prefers-color-scheme` u `boot.js` (F1/3), a nitko ne zna je li aplikacija INSTALIRANA (Leon testira baš nju). **Preduvjet za F1/12–F1/14:** `boot.js` (već odlučuje prije crtanja) izračuna **`dodir`** (`pointer: coarse`) · **`hover`** · **`hibrid`** (laptop s dodirom: `any-pointer: coarse` + `hover: hover`) · **`razred`** širine (telefon / tablet / stolno, isti pragovi kao CSS) · **`os`** (iOS samo gdje je nužno — `GestureEvent`, isti test kao no-zoom.js) · **`pwa`** (`display-mode: standalone`) → upiše `<html data-uredjaj="dodir hover …">` + zamrznut `window.SokratUredjaj`, i osvježava na `matchMedia(...).change` (tablet dobije miš). **Ugovor:** CSS pita `:root[data-uredjaj~="dodir"]`, JS pita `SokratUredjaj.dodir` — **nitko drugi ne zove `matchMedia` za pointer/hover** (brana: statički test nad `js/**` i `css/**`; F1/9-ov `@media (pointer: coarse)` seli na atribut u istoj cigli). Tutorial (F1/14), red gumba (F1/12), tipke (F1/13) i birač tema (§6/7) čitaju odavde. Brane: `vm`-pješčanik s lažnim `matchMedia` (svaka kombinacija → točan atribut) · phone-gate tvrdi `dodir` na iPhone profilima · `theme-boot-order` (boot ostaje sinkron, prije CSS-a). U konzoli `SokratUredjaj` kaže što platforma misli da je uređaj — to Leon vidi na telefonu. | …svaki dio platforme koji se na telefonu ponaša drukčije pita JEDNO mjesto, a Leon na svom iPhoneu vidi da platforma zna: dodir, bez hovera, telefon, instalirano |
| **F1/12** | **Tinder-KADAR: kartica = ekran, ✓ / ✕ pod palcem** (Leon, 2026-09-06 navečer, s previewom F1/9: *„Treba kartica biti veća i trebamo promijeniti veličinu kartica da budu kao na Tinderu. Ovo je velik posao i prvo trebamo napraviti mjerenje i plan."*). **Izmjereno 06.09.** (`BACKLOG.md` §E, 4 profila, sigurna zona kao phone-gate): na 393×852 kartica je **353×200 px = 23 % ekrana, 34 % dostupnog** prostora između gornjeg kroma (y 167) i donje navigacije (y 755 → 588 px); stranica u modu kartica **skrola** (1045 px); pitanje **12,8 px**, odgovor **12 px**, objašnjenje 10,4 px; četiri gumba 50 px u dva reda (112 px), h1 27 px, stats 21 px. Gradivo (2 773 kartice, 24 predmeta, bez `final`-kopija): pitanje med 48 / max 134 zn., naličje (odgovor + objašnjenje) med 225 / p95 474 / max 656 zn. → pri **16 px** naličje traži med 194 / p95 340 / max 398 px na 353 px širine. **Cilj:** širina `vw − 32`, visina = dostupno − tanka traka napretka − red gumba (≈ **464 px na 393**, 287 na SE 375, 544 na 430 = 70–81 % dostupnog); **≥ 60 % dostupnog, ≥ 90 % širine, stranica NE skrola**; h1 na telefonu otpada (mod je već u navigaciji), brojke „znam / ne znam" = značke na gumbima; pitanje 18–20 px, odgovor 16 px, objašnjenje 14 px; **fiksna visina + unutarnji skrol naličja** (na 393 i 430 stane i najduže, na SE p95 traži skrol — izmjereno). **Gumbi kao Tinder:** dva okrugla ispod kartice (✕ = ne znam · ✓ = znam, 56–64 px, puna ispuna danger/ok, ADR-032), prev/next na dodiru nestaju (lista se palcem, F1/13). **I na kompu isti red** (Leon, 2026-09-06: *„dobro bi bilo da i na kompu imamo strelicu, i strelica treba biti desno a ✕ lijevo"*) — pretpostavka koju Leon potvrđuje u cigli: red **← · ✕ · ✓ · →**, ✕ lijevo od sredine, strelica „sljedeća" krajnje desno; na dodiru samo **✕ · ✓**. Stolna kartica zadržava današnju veličinu (`css:diff` izvan `pointer: coarse` = 0 razlika osim reda gumba). Radi se u DVIJE dionice s commitom: ① kadar (visina, fontovi, h1/stats) ② gumbi. Brane: phone-gate dobiva mjeru **„u modu kartica kartica ≥ 60 % dostupnog i stranica ne skrola"** (danas crvena = dokaz da mjeri) · `check:contrast` za nove gumbe · a11y (ime gumba = i18n, ne ikona). | …Leon na telefonu vidi karticu koja je ekran, čita ju bez zumiranja, a ✓ / ✕ su mu pod palcem kao lajk i ✕ |
| **F1/13** | **Palac LISTA, gumbi SUDE** (Leon, 2026-09-06: *„Ako se povuče lijevo vraća se na prijašnju, desno ide na sljedeću. Kada se okrene daje odgovor. Know i don't know stoje dolje kao što Tinder ima lajk i ✕."*). Mijenja ZNAČENJE geste iz F1/9, ne mehaniku: povlačenje **desno = sljedeća** (`nextCard`), **lijevo = prethodna** (`prevCard`), dodir = okreni (ostaje na `pointerup`); **✓ / ✕ = `markKnown` / `markUnknown`** s pečatom i letom naprijed (oboje ide na sljedeću, kao i danas). Sjene špila: desno vire sljedeće; kad se ide natrag, prethodna se vraća s lijevog ruba. Rubovi: prva kartica + lijevo = odskok; **zadnja + desno = IZBORNIK KRAJA ŠPILA** (Leon, 2026-09-06, §6/8): *ispočetka* · *promiješaj* · *ponovi ne-znam* (špil samo iz `cards.unknown`) — sva tri idu kroz `initFlashcards`-put (novi špil = `resetSwipe`), nijedan ne dira upisani napredak; izbornik je i18n, fokusabilan, zatvara se Escapeom i vraća na zadnju karticu. **Stolno = SVE tipkama** (Leon: *„da se sve može s tipkama kontrolirati"*): ← / → = prethodna / sljedeća, razmak / Enter = okreni, **X = ne znam · Z = znam** (Z kao „znam", X kao ✕; QWERTZ ima oboje), u izborniku kraja strelice + Enter; miš klika isti red gumba kao na dodiru. **Predviđeno za F1/14:** akcije žive u JEDNOJ tablici u `flashcards.js` (`id → gumb · gesta · tipka · i18n ključ`) iz koje se crtaju gumbi, vežu tipke I sastavlja tutorial — jedna činjenica, jedno mjesto (ADR-027). **Cijena:** `flashcard-swipe.test.js` i `flashcard-swipe.spec.js` se **PREPISUJU** na novo značenje — staro „desno = znam" ne smije preživjeti ni u jednoj brani; F1/9-ova mehanika (pointer-put samo za dodir, `touch-action: pan-y` i na skrolerima, `pointerup`-okretanje, rezervni timer, `gen`) ostaje netaknuta. Ovisi o F1/12 (gumbi ✓ / ✕ moraju postojati da bi prev/next smjeli nestati). | …Leon palcem prolazi špil naprijed i natrag, okreće dodirom, sudi gumbima — a „znam / ne znam" je isto stanje koje vide statistika i cloud-sync |
| **F1/14** | **Lagani TUTORIAL pri prvom ulasku u kartice — zaseban za telefon i za komp** (Leon, 2026-09-06: *„kasnije bi trebali razraditi da platforma napravi lagani tutorial kada se uđe na kartice, poseban za kompjuter, poseban za mobitel — platforma treba to predvidjeti"*). **Kasnije**, ali F1/12 + F1/13 ga PREDVIĐAJU: tablica akcija (v. F1/13) je jedini izvor, pa tutorial ne nosi drugu kopiju natpisa; ulaz ima jedno mjesto (`switchSection("flashcards")`) → biljeg „viđeno" po uređaju u `localStorage` (uz račun F2 seli u profil); tekstovi kroz `i18n.js`; nikad modal koji blokira (ADR: `<sokrat-modal>` samo za odluke) nego 2–3 koraka nad karticom s „preskoči"; `(pointer: coarse)` bira mobilnu varijantu (povuci · dodirni · ✕ ✓), inače stolnu (← → · razmak · X Z). Brane: a11y (fokus, Escape), i18n, phone-gate (tutorial ne smije probiti kadar iz F1/12). Ista ideja kasnije vrijedi za kviz i dopune — ovdje samo kartice. | …student koji prvi put otvori kartice za 10 sekundi zna kako listati, okretati i suditi, na svom uređaju, i nikad to više ne vidi |

**Zamke koje su već poznate:** `perf-probe` ovdje **ne pomaže** — on mjeri prvi kadar, a trzanje je
trošak po kadru. ⚠️ **`theme-color` meta je i dalje statična svijetla** (`#f7f9fc`, F4): na tamnom
telefonu je traka preglednika svijetla iznad crne stranice — jedino što F1/3 nije zacrnio. Popravak
bez druge kopije boje: `boot.js` nakon primjene teme pročita `--color-surface-0` iz izračunatog stila
(sinkrona skripta iza stylesheeta ga već ima) i upiše u `meta.content`. Indigo je u obje teme granični slučaj, i to **na suprotne strane** (na svijetlom
tekst traži tamniji, na crnom svjetliji) — brana to hvata samo ako tema uopće uđe u njezin popis,
zato F1/4 nije uredovanje nego dio posla.

**Što ova faza svjesno NE dira:** `chalk` i `mint` ostaju kakvi jesu, kao izričiti izbori.
Odluka da je **zadana svijetla** (2026-08-13, jer su *„dvije tamne palete zaredom pale na živom
ekranu"*) **ostaje na snazi** — crno dobiva onaj tko ga je uređajem već tražio, ne svaki posjetitelj.

**Izlaz iz faze:** Leon otvori stranicu na svom telefonu i potvrdi petero — crna je, nema bljeska, ništa ne svijetli poslije dodira, **ništa ne zumira** (ni štipanje ni dodir ni polje — ADR-034),
nema trzanja. To je jedini prihvatljiv dokaz; mjera na razvojnom stroju je donja granica.

---

### F2 · RAČUN

Nastavak bloka koji je R1 otvorio. **Profil i `css/profile.css` su ista površina**, pa cigla koja
prepisuje profil nosi i njegov CSS — inače se ista datoteka prepisuje dvaput.

### 🎯 Oblik profila je odlučen (Leon, 2026-09-08)

*„Profil mora biti na isti način kao i Facebook, i privatni sadržaji koje korisnik radi; kasnije će se
expandat da se može objavljivat sadržaj."*

**Profil postaje ZID, radionica ostaje radionica — i to ne ruši ADR-029, nego ga izvodi.** Facebook sam
radi oboje: objave stoje na profilu, ali ih pišeš i slažeš drugdje. Isto se preslikava ovdje:

| površina | uloga | što je unutra |
|---|---|---|
| `#profile-page` | **tko si i što si napravio** | naslovna, avatar, ime, opis, brojke, **zid materijala**, pa postavke |
| `#materials-page` | **gdje to gradiš** | polica (skinuto) + stablo mapa + uređivanje |

⚠️ **ADR-029 ostaje netaknut**: vlastito gradivo je i dalje **ravnopravno odredište**, ne pododjeljak
profila. Od 2026-09-08 ima i **ulaz u traci na svakoj stranici** (F2/0), pa je istaknutije nego ikad.

**Zid je gotovo besplatan:** `nodes` već nosi `name`, `icon`, `color`, `updated_at`, `kind='study'` — kartice
zida dobivaju izgled bez ijednog novog stupca (boja se ionako nasljeđuje od sekcije, ADR-025).

⚠️ **Identitet NE SMIJE u tablicu `profiles`.** Ona ima točno `user_id · role · created_at` i iz nje čita
`is_admin()`, kojeg zovu RLS-politike. Dvije zamke: ① politika za samo-uređivanje daje korisniku pravo da
si upiše `role = 'admin'` osim ako se to izričito zabrani; ② F7 traži **javno čitanje** profila, a javna
`SELECT`-politika nad `profiles` odala bi **tko su administratori**. Zato ime/opis/slike idu u **zasebnu
tablicu javnog identiteta**, uz upis kroz `SECURITY DEFINER` RPC (kao svaki upis u ADR-024).
`user_metadata` (gdje `display_name` stoji danas) radi savršeno dok si sam sebi publika, ali je u tvom
JWT-u i **nitko ga drugi ne može pročitati** — pa F7 ionako traži tablicu; jeftinije je sad.

**Javnog pogleda na profil NEMA u F2.** Danas nitko ne može vidjeti tuđi profil; zid je privatan, a
javnu stranu otvara **F7 objava** (ADR-035/036). U F2 se gradi oblik, ne publika.

| cigla | posao | gotovo kad |
|---|---|---|
| **F2/0** ✅ | **Traka dobiva dva odredišta** — „Moji materijali" + profil; CTA „Počni učiti" obrisan (duplikat: tri `.start-trigger`-a ostaju na landingu). Dodir dignut na 44×44. **Isporučeno 2026-09-08.** | …u vlastito gradivo se ide sa **svake** stranice, a ne samo s landinga ili iz profila |
| **F2/1** ✅ | **Tema prati račun.** `localStorage` ostaje **prvi kadar** (odluka mora pasti prije crtanja, `boot.js`), račun postaje izvor istine koji ga pri prijavi pregazi i pri promjeni upiše. **Odjava BRIŠE lokalni izbor** (Leon, 2026-09-06: *„tuđi izbor ne smije preživjeti odjavu"*) — račun ga čuva za iduću prijavu. Birač: natpis samo **„Automatski"**, bez sufiksa s trenutnom temom (§6/7) — **✅ natpis isporučen 2026-09-06** (`feat/nocna-b`, spojeno 12.09.). **① ✅ 2026-09-14** (`feat/f2-tema-racun`): račun = izvor istine (`user_metadata.theme`, „Automatski" = `auto`), odjava briše lokalni izbor, račun bez teme preuzme izbor uređaja nakon svježeg `getUser()`; brane `theme-account.test.js` (32) + `theme-account.authed.spec.js` (STAGING), obje obrnuto crvene. **③ ✅ 2026-09-14** (`feat/f2-avatar-traka`): profilna u traci iz JWT-a (`user_metadata.avatar_path` = BACKLOG §🌐 A4; zrcali `profile-images.js`, samopopravak u `loadIdentity`, putanja se provjerava prije URL-a); usput `USER_UPDATED`/`TOKEN_REFRESHED` više ne crtaju profil preko otvorene forme. Brane `profile-images.test.js` +7 + `topbar-avatar.authed.spec.js`. **④ ✅ 2026-09-14 — odjava samo OVAJ uređaj** (Leon, anketa 14.09.: *„Samo ovaj uređaj"*; `feat/f2-tema-racun`): `SokratAuth.signOut()` je zvao `client.auth.signOut()` bez `scope`, a u zakucanom supabase-js 2.110.8 je to `global` → odjava na mobitelu odjavila je i računalo. Sad `signOut({ scope: 'local' })` (tema se i dalje briše lokalno — `SIGNED_OUT` stiže i za lokalnu odjavu); brisanje računa (`delete-account`) ostaje globalno. Brane `signout-scope.test.js` + `signout-local.authed.spec.js` (STAGING, dvije prave sesije), obje obrnuto crvene. | …korisnik postavi temu na jednom uređaju i zatekne ju na drugom čim se prijavi |
| **F2/2** (iza 3b) | **Profilna slika + naslovna** — bucket po obrascu `node-images`: vlasnički prefiks + RLS. ⚠️ **Ide IZA F2/3b** (Leon, 2026-09-09): putanja se upisuje u `profile_identity`, pa bi prije toga završila u `user_metadata` i selila se dvaput. **ODLUČENO (Leon, 2026-09-09): JAVAN bucket, upis samo vlasniku.** Avatar postoji da bi ga se vidjelo, F7 ga ionako otvara, a potpisani URL bi tražio krug prema bazi prije svakog crtanja i ne bi se kesirao na CDN-u. Cijena je izgovorena: tko ima URL, vidi sliku i bez prijave. Ime datoteke nosi uuid da se stara slika ne da pogoditi nakon zamjene; slika se smanjuje U PREGLEDNIKU prije uploada (avatar ~512 px, naslovna ~1500 px, WebP) — inače fotka s iPhonea udari u zid od 5 MB, a HEIC nije na popisu dopuštenih tipova. **Cigle:** ① baza ✅ **13.09.** (`supabase/f2-profile-images.sql` + RPC `set_profile_image` + `delete-account` čisti oba bucketa; STAGING, brana `profile-images.authed.spec.js` crveno→zeleno) · ② klijent ✅ **13.09.** (`js/profile-images.js`: odabir, canvas → WebP, upload, RPC, brisanje stare; unit 18 + spec ④ u Chromiumu) · ③ zid crta slike + gumbi ✅ **13.09.** (spec ⑤ klik-put, `phone.authed` 12/12) · ④ brane ✅ (nošene ciglama 1–3, ne zasebno). **🚀 NA PRODUKCIJI 13.09.** (`61c39dd`). **Dopuna (Leon 13.09.):** „+" na portretu · gumb na naslovnoj · **izrez** (`js/image-crop.js`, 1:1 / 3:1) — ✅ na grani, čeka push. | …korisnik stavi svoju sliku i vidi ju odmah, a tuđi prefiks mu je nedostupan |
| **F2/3a** ✅ | **Kostur zida** — naslovna, avatar, ime, opis, `[Uredi profil]`; postavke (tema, lozinka, GDPR, odjava) sele **ispod** zida. **Isporučeno 2026-09-09**; usput obrisan unutarnji naslov „Moj profil" (duplikat trake razine) jer je zid gurao prvu kontrolu ispod pregiba na 320×568. Ime i opis privremeno u `user_metadata` — F2/3b ih seli. | …profil se otvara kao stranica o korisniku, a ne kao popis postavki |
| **F2/3b** ✅ | **Spremište identiteta** — zasebna tablica + `SECURITY DEFINER` RPC koji **nikad ne dira `role`**. **Isporučeno 2026-09-09** (`supabase/f2-profile-identity.sql`; na PROD-u od 13.09.). Stupci `avatar_path`/`cover_path` upisani unaprijed za F2/2. | …korisnik promijeni ime i opis, a `profiles.role` ostaje nedodirljiv (obrnuto provjereno) |
| **F2/3c** ⤵ F2/5 | ~~**CSS profila i susjeda** — `css/profile.css`, `auth.css`, `pages.css`, `consent.css`, `legal.css`, `home-section.css`, `sidebar.css` (bivši C6).~~ **SPOJEN u F2/5 (2026-09-14, Leon: *„možemo dalje"* na preporuku):** C6 je **zatvoren 2026-09-01** (`docs/archive/FRONTEND_REDIZAJN.md` §13.9, 1 166 880 usporedbi / 0 razlika) — selidba bi ponovila gotov posao; `legal`/`consent` mjerom isključeni (§13.1), `sidebar.css` briše F4 (§6/2), ostatak komponenti je F4/3. Ostaje samo kriterij „profil izgleda kao ostatak platforme" — a zid se dotjeruje JEDNOM, na konačnom obliku (F2/5). Prvi dio već isporučen 14.09. (`aca5ed1`: gumb naslovne u kutu, naslovna 3:1 = izrez, portret 112 px na telefonu). | …profil izgleda kao ostatak platforme |
| **F2/4** | **Mail-obavijesti** — Edge Function (ADR-016), pristanak iz upitnika, odjava jednim klikom iz maila, admin-forma. Prvi segment: FMTU. **ODLUČENO (Leon, anketa 15.09.):** forma u admin-kartici profila · prekidač „Obavijesti mailom" u postavkama profila (povlačenje pristanka jednako lako kao davanje; 4/5 PROD računa upitnik nikad nisu vidjela) · segmenti „FMTU" i „svi s pristankom". **Oblik:** `send-notification` (JWT + `is_admin()`; primatelji = `mail_consent === true` + potvrđena adresa + segment; Resend, `sokrat@sokratstudy.com`, zaseban mail po primatelju; broj primatelja prije slanja; „proba meni") · `mail-unsubscribe` (bez JWT-a, HMAC-potpis koji ne istječe) · odjava = `List-Unsubscribe` one-click (RFC 8058) + poveznica na NAŠU `odjava.html` s jednim gumbom (GET ne odjavljuje — skeneri pošte otvaraju poveznice; Supabase ne poslužuje HTML iz funkcija) · `mail_log` (jedan red po slanju, bez adresa). **Cigle:** ① jezgra `supabase/functions/_shared/mail-core.ts` + `f2-mail-log.sql` (STAGING) ✅ 2026-09-15 · ② obje funkcije na STAGINGU + `npm run test:mail` ✅ (tajne postavljene 15.09.: T0–T6 zeleni, bez SKIP-a) · ③ `odjava.html` + prekidač + admin-forma ✅ · ④ „16+" (kvačica na mail-putu, rečenica uz Google; Leon, anketa 15.09.) ✅ · ⑤ Pravila privatnosti u `privacy.html` ✅ (Leon odobrio). **Sve na grani `feat/f2-mail`, PROD čeka Leona:** SQL `f2-mail-log.sql` → tajne na PROD-u (drugačiji `MAIL_UNSUB_SECRET`, **bez** `MAIL_REDIRECT_TO`) → deploy obje funkcije (`mail-unsubscribe` bez JWT-a) → `check:functions` zelen → push (s Pravilima). | …primi mail o novom predmetu SAMO ako je pristao, i odjavi se jednim klikom iz samog maila |
| **F2/5** ✅ | **Zid + radionica** — rešetka vlastitih materijala na profilu (`nodes`, `kind='study'`) i `css/my-materials.css` za policu i stablo. Isti sadržaj, dva pogleda. **Plan 14.09. (izmjereno snimkom 393 px: svaki redak stabla nosi PET ikona, imena odrezana na 6–8 znakova):** **5a zid** — rešetka do 6 zadnje mijenjanih materijala (2 stupca telefon / 3 stolno; boja, ikona, ime u 2 retka, mapa), dodir = učenje, „Svi materijali →", prazno stanje s „Napravi prvi"; bez promjene baze · **5b radionica** — redak = ikona u boji + PUNO ime + jedan „⋯" (Uči · Uredi · Preimenuj · Premjesti · Obriši), dodir na redak = glavna radnja, ručka za povlačenje samo na `pointer: fine`, jedan „+ Novo" · **5c** — polica istog izgleda + makni DRUGI gumb profila iz zaglavlja `#materials-page` (traka ga već ima). ✅ **ODLUČENO (Leon, anketa 14.09.):** ① dodir na materijal = **UČENJE** — i na zidu i u radionici, isto ponašanje na oba mjesta; uređivanje, preimenovanje, premještanje i brisanje kroz „⋯" · ② premještanje na telefonu **kroz izbornik** („⋯ → Premjesti u…" → popis mapa); povlačenje ostaje samo mišem (`pointer: fine`) · ③ F2/3c **spojen** ovamo (potvrđeno). **5a ✅ 2026-09-15** (`feat/f2-zid`): kartica „Otvori moje materijale" zamijenjena rešetkom (`profile.js` `shelfHtml`/`fillShelf`, podaci `SokratMaterials.loadWall` → čisti `recentStudy`). „Zadnje mijenjano" = **`node_content.updated_at`** (bez njega vrijeme stvaranja) — NE `nodes.updated_at`, jer `reorder_nodes` dira svu braću i zid bi skakao od presložavanja. **Boja (Leon, anketa 15.09.: *„stalna boja iz palete"*):** materijal bez vlastite boje (danas svi — nema sučelja ni RPC-a za `nodes.color`) dobiva stalnu boju iz kurirane palete Studija, izvedenu iz id-a (`bojaMaterijala`, `js/utils.js`); ista boja ide i na stranicu učenja tog materijala. ⚠️ `studio.js` `SECTION_COLORS` i `block-editor.js` `BLOCK_COLORS` još nose svoje kopije palete → zasebna mala cigla. Brane: `my-materials.test.js` +10 · `profile-shelf.authed.spec.js` (①–④, obrnuto: 6/6 pada na starom kodu). **5b-1 ✅ 2026-09-15** (redak): ikona u boji materijala + PUNO ime (lomi se) + jedan „⋯" (`popover`, DOM u retku → stari `data-mm-*` rukovatelji netaknuti); dodir na redak = učenje / otvori-zatvori mapu / prazna mapa → „⋯"; ručka samo na `pointer: fine`. Brana `radionica.authed.spec.js` (①–③, i u WebKitu) — ulovila da drugi dodir na „⋯" ponovno otvara izbornik (popover se zatvara već na pritisak). **5b-2 ✅ 2026-09-15** („Premjesti u…"): stavka u „⋯" → `<sokrat-modal>` s popisom polica (čisti `moveTargets`: vrh + žive police, bez sebe i potomaka, trenutna onemogućena) → isti RPC `move_node` kao povlačenje. Brane: `my-materials.test.js` +5 · `radionica.authed.spec.js` ④ (stolno) + ⑤ (dodir), i u WebKitu · `a11y.authed` skenira otvoren izbornik i prozor u 5 tema. **5b-3 ✅ 2026-09-15** (jedan „+ Novo"): dva gumba trake → jedan koji otvara izbornik istog oblika („Novi materijal" prvi, pa „Nova polica"); izbornik stoji uz bliži rub gumba. Brana `radionica` ⑥ + ③ (telefon: uz lijevi rub, obrnuto pada). **5b = GOTOV.** **Pregled 15.09.** (Leon: *„prođi kroz sve"*): šest nalaza popravljeno (zid trepće na izbor teme · strelice · Tab · pomak · put do podpolice · fokus), tri sumnje oborene provjerom — CHANGELOG. **5c ✅ 2026-09-15:** ① izbornik izvučen u `js/row-menu.js` (jedno ponašanje, vlasnik nosi `data-menu-scope`) · ② polica skinutog istog izgleda (ikona u boji predmeta, puno ime, „⋯" s Osvježi/Ukloni, redak = poveznica K1) · ③ drugi gumb profila maknut iz zaglavlja `#materials-page` (brana u `materials-entry.spec.js`). **F2/5 = GOTOV** (na `feat/f2-zid`). | …korisnik otvori svoj profil i vidi što je napravio, a materijali izgledaju kao proizvod, ne kao administracija |

⚠️ **F2/3 je namjerno razrezan na tri.** Kao jedna cigla dirao bi zajednički šav (sedam CSS datoteka +
novo spremište + novi raspored) — točno onaj oblik zahvata na kojem je tempo već dvaput pukao. Commit
ide nakon svake, gate na svakoj.

**Ne popušta ni ovdje:** identitet isključivo iz JWT-a (`getUser()`) · `service_role` samo u Edge
Functions · RLS i `publish_document` nedirnuti · osobni graditelj ostaje zaseban otok.

**Izlaz iz faze:** cijeli krug računa radi na pravom uređaju — prijava, tema, slika, ime, mail —
i korisnik otvori **svoj profil** i ondje vidi što je napravio, u obliku koji ne izgleda kao alat.

---

### F3 · DVOJEZIČNOST

Brana `check:i18n` već postoji i broji po datoteci; **dokaz napretka je spuštanje osnovice**, nikad
procjena. Redoslijed ide po tome gdje je posao najgušći i najizoliraniji.

| cigla | posao | gotovo kad |
|---|---|---|
| **F3/1** | **Četiri pravne stranice** — ~~nemaju ni mehanizam: prvo `js/i18n.js` + `data-i18n`, pa prijevod~~ (mehanizam presudio Leon: oba jezika u stranici, ADR-037). Najveći pojedinačni komad. ~~⚠️ Iste stranice dobivaju temu u F1/2 — ako F1 još nije zatvoren, spojiti u jedan obilazak.~~ Tema isporučena F1/5 (2026-09-06). ⚠️ **Dopuna (Leon, 2026-09-06: *„Zapiši: trebamo promijeniti FAQ, about us srediti malo, i srediti da je glavni kontakt sokrat@sokratstudy.com"* — uz *„samo zapiši, nemoj ništa raditi"*): SADRŽAJ PRIJE PRIJEVODA, u istom obilasku** — ① **glavni kontakt `sokrat@sokratstudy.com`** umjesto Gmaila — ✅ **ISPORUČENO 2026-09-16 na grani** (`feat/f3-dvojezicnost`): 8 `mailto:` poveznica (`contact` · `faq` ×3 · `privacy` ×2 · `terms` · `index.html` about) + `README.md`; brana `tests/unit/kontakt-adresa.test.js` (u `test:unit`, na starom tekstu 21 pad). Gmail namjerno OSTAJE u `scripts/mail-preview.js` (testni podatak) i `supabase/f4-admin.sql` (Leonov račun, ne kontakt). Porkbun prosljeđivanje na Gmail je uključeno — ✅ **isporuka POTVRĐENA (Leon, 15.09.: *„sve što stigne na sokrat@sokratstudy.com dolazi na moj mail leonkreso784@gmail.com, već je testirano"*)**, pa zamjena smije ići) ② **FAQ — novi tekst** ③ **About us dotjerati** (`#/about` u `index.html`, ne pravna stranica) — ✅ **②+③ ISPORUČENO 2026-09-16 na grani** po Leonovoj presudi nacrta (*„super, možemo nastaviti"* + anketa): FAQ 12 pitanja, „O nama" (točan opis kataloga, oznake = stvarni načini učenja, „Get in touch", „Founder & developer", glas = množina), Kontakt bez „pošalji bilješke", Pravila §7 „Delete study history" (ime gumba), vrata naslovnice bez obećanja dijeljenja (`door.make.m`). **Uvjeti korištenja dobivaju NOVI tekst prije prijevoda** (Leon, anketa 16.09.: ne znaju za vlastito gradivo, Google-prijavu ni 16+) — nacrt čeka Leonovu presudu. **MEHANIZAM (Leon, anketa 16.09.): OBA JEZIKA U STRANICI** — `<div class="jezik" lang="en|hr">`, `boot.js` bira prije crtanja, `check:i18n` presuda ④ traži isti oblik; ne rječnik (i18n.js 75 KB na putu svakog posjetitelja + nov innerHTML-put) ni zasebne stranice. ✅ **③a Kontakt dvojezičan** (osnovica `contact.html` → 0). ✅ **③b FAQ dvojezičan** (`faq.html` → 0). ✅ **③c Pravila privatnosti dvojezična** (`privacy.html` 150 → 0). ✅ **③d Uvjeti: novi tekst v2 + HR** (`terms.html` 40 → 0). **F3/1 = ✅ GOTOV NA GRANI 2026-09-16** (sve četiri pravne stranice izvan osnovice; `check:i18n` 474 → 224). **Uvjeti v2 ODOBRENI (Leon 16.09.)** — računaju s dijeljenjem linkom (F7/1) i javnom objavom (ADR-035); ⚠️ pri F7 dopuniti i Pravila privatnosti. Red: FAQ → Pravila privatnosti → Uvjeti (poslije presude nacrta). Redoslijed nije ukras: prijevod tek poslije novog teksta, inače se prevodi dvaput. **Tko piše (Leon, 15.09.):** nacrt FAQ-a i About usa pišem JA (provjeren prema stvarnom stanju: UGC, materijali, profil, obavijesti), Leon čita i presuđuje — isti put kojim su išla Pravila privatnosti. **Pravila privatnosti su od 15.09. NOVI tekst** (`privacy.html`, `feat/f2-mail`) → prevodi se TA inačica, ne starija. | …korisnik na hrvatskom otvori Pravila privatnosti i pročita ih na hrvatskom |
| **F3/2** | **Zakucan tekst u datotekama s mehanizmom** — `index.html`, pa redom po težini. ✅ **Cigla 1 (16.09., na grani):** `index.html` 49 → 21 · `editor.html` 5 → 0 · `sokrat-toast.js` 1 → 0 (ukupno 224 → 190). **Pravilo cigle:** mjesto koje JS UVIJEK piše (kartica, kviz, naslovi, toast) ostaje u markupu PRAZNO, ne dobiva `data-i18n` — `applyTranslations()` bi ga na promjenu jezika pregazio (brana: `i18n.spec.js` F3/2, obrnuto provjereno). Novi mehanizam `data-i18n-title`; oznaka prekidača ide kroz ključ `topbar.langCode`, zaseban pisac u `i18n.js` uklonjen. Ime prekidača dvojezično u oba jezika (Leon, 16.09.). ✅ **Cigla 2 (16.09., na grani): slijepa karta** — `index.html` 21 → **0** · `js/blind-map.js` 8 → 0 (ukupno 190 → 161). Poruke iz stanja (koordinate, rezultat) precrtava kuka `renderBlindMapText` u `applyTranslations`; poruka s imenom mjesta ide kao DOM, ne `innerHTML`; zadatak više ne piše „Find:" dvaput. **Odluke (Leon, anketa 16.09.):** ① **Zadani jezik prati UREĐAJ** — bez spremljenog izbora: uređaj na hrvatskom (`hr`, `hr-HR`) → hrvatsko sučelje, SVE ostalo → engleski (*„nemamo veze sa srpskim niti crnogorskim niti bosanskim, eng i hrvatski su jednako bitni"*); odluka prije prvog crtanja u `boot.js` (isti put kao tema, F1/3), prekidač pobjeđuje i pamti se; `odjava.js` već ima vlastitu provjeru `hr` → seli u to jedno mjesto. Mijenja SAMO sučelje: *„predmeti i lekcije i sve treba ostati na jeziku kojem je napravljeno"* (ADR-012). **= cigla 3, prije vježbi.** ② **Promjena jezika usred aktivnosti: trajni tekst se precrtava ODMAH** (napredak kviza, rezultat, koordinate — kuka po načinu učenja, uz ciglu te datoteke, kao `renderBlindMapText`); kratke poruke (1–2 s) ne. ③ Ime prekidača na pravnim stranicama (`legal.lang`, jednojezično) vs traka (`topbar.lang`, dvojezično) — pitano dvaput, bez presude → ostaje kako jest. ✅ **Cigla 3 (16.09., na grani): jezik prati uređaj** — `boot.js` `jezikUredjaja()` (prvi jezik uređaja, samo `hr`), `i18n.js` pita istu funkciju, kopija u `odjava.js` uklonjena; Playwright `locale: 'en-US'` zakucan (jezik računala je od sada ulaz u testove). Brane: `jezik-boot.test.js` ④ (na starom kodu 8 padova) · `i18n.spec.js` „jezik uređaja" (hr-HR · pravna stranica · sr-RS; obrnuto: stari kod → 2 pada). ⚠️ Playwright ne vidi kvar SAMO u `boot.js` (i18n ga nadoknadi poslije učitavanja) — prije-prvog-crtanja čuva unit test. **Red dalje (ako MCP ne preskoči, v. §2):** `exercises.js` → `profile.js` → `my-materials.js` → `learn.js`/`auth.js` → `progress.js` → … (editorske datoteke su F3/3). **Brana ne vidi, a treba uzeti uz tu datoteku:** ~~`js/auth.js` `aria-label` gumba za prijavu („My profile"/„Sign in", ternarni izraz)~~ (✅ 4a) · ~~`js/learn.js:155` engleski rezervni `alt` **i** prefiks „Open image:" uvećane slike~~ (✅ 4b, uz vidljiv natpis „Learn image" u pregledniku slike). **Cigla 4 (red presudio Leon 16.09., prije MCP-a) = četiri commita (Leon 17.09.):** ✅ **4a prijava (17.09., na grani)** — imena gumba (zatvori, oko lozinke u prozoru i profilu, gumb u traci) kroz ključeve; prozor za prijavu nosi `data-jezik` i **pregrađuje se pri otvaranju** kad se jezik promijenio, upisano ostaje; brana ③ zna `pt`/`at`, a `setAttribute(atr, uvjet ? 'A' : 'B')` se sudi; `check:i18n` 161 → 154. → ✅ **4b mjerni ispravak (17.09., na grani)** — **Leon presudio „A": engleska rezerva uz ključ koji POSTOJI u rječniku se ne broji** (rezerva uz nepostojeći ključ i goli tekst i dalje padaju); brana zna sve kućne helpere (izmjereno: +`ct`/`tr`/`T`/`_t`/`_pt`), `setAttribute` i `askConfirm` sude svaki literal argumenta; `learn.js` slika prevedena (i vidljiv natpis preglednika); `check:i18n` 154 → 124 (29 mjera, 1 prijevod) → ✅ **4c profil (17.09., na grani)** — prekidač precrta profil ODMAH, a otvorene forme i upisano vrati (`renderProfileText`); „Član od" s mjesecem na jeziku sučelja (brana nije vidjela); greška pri promjeni lozinke kroz `SokratAuth.authError`; zastarjela rezerva ispravljena; brojka ostaje 124 (kvarovi ponašanja) → ✅ **4d materijali (17.09., na grani)** — stranica i polica se na prekidač precrtaju iz već učitanih podataka (upisano ime ostaje, greška ostaje greška), „Odustani" u „Premjesti u…" upisuje se pri otvaranju; brojka ostaje 124. **Cigla 4 = GOTOVA → sljedeće F6 MCP (plan pa Leonov OK).** **Brana ne vidi, uzeti uz tu datoteku (nađeno u 4d):** gumb „Download for offline" na stranici lekcija (`offline-store.js` `mount`) ne precrtava se na prekidač. | …korisnik prebaci jezik na naslovnici i ne vidi engleski ostatak |
| **F3/3** | **Editor dvojezično (K5)** — ključevi koje kod zove, a rječnik ih nema: `at()` tiho vraća engleski i ništa ne pukne. Smjer je **upisati ih u rječnik**, ne brisati pozive. | …autor uređuje gradivo na hrvatskom sučelju |

**Tvrdo pravilo koje ne pada:** mijenjanje jezika **sučelja** NIKAD ne dira **predmete** — jezik
gradiva je svojstvo programa (ADR-012).

**Izlaz iz faze:** `scripts/i18n-baseline.json` je prazan i brana traži nulu — isti obrazac kojim
je zatvorena faza TELEFON.

---

### F4 · ČIŠĆENJE

Faza u kojoj se **ne smije promijeniti nijedan piksel**. Zato ima najstrožu obrnutu provjeru:
`css:diff` i `blocks:diff` moraju pokazati nulu razlika, a ne „izgleda isto".

| cigla | posao | gotovo kad |
|---|---|---|
| **F4/1** | **Gradivo i vježbe** (bivši C5b) — `learn.css` · `learn-blocks.css` · `math.css` · `exercises.css` · `blind-map.css`. ⚠️ `learn.css` je sagrađen na `#learn`, pa utility ne prolazi dok ID stoji. | …student čita gradivo i rješava vježbe, a KaTeX, slike i tablice su nedirnuti |
| **F4/2** | **Studio na telefonu** (bivši C3, jedini razlog zašto ta cigla nije zatvorena). | …autor uredi lekciju na 320 px bez elementa koji strši ili je prekriven |
| **F4/3** | **Gašenje** (bivši C7) — `responsive/*`, `components.css`, `variables.css`, **`styles.bundle.css` obrisan**, mrtva tema. Nosi i dva nalaza koja mijenjaju prikaz pa nisu smjela ranije: `.reset-btn` bez `font-size` i `.category-bar-info span` koji pogađa dva elementa. | …u repozitoriju nema starog CSS-a, mrtvog koda teme ni ijednog `!important` |
| **F4/4** | **Ekran 568 × 320 ulazi u branu.** Moguće TEK nakon F2/3 (consent banner) i F4/3 (donja traka) — dotad bi cigla platila tuđim crvenilom. Brojke su izmjerene, ne treba mjeriti iznova. | …`phone-gate` mjeri i najmanji polegnuti ekran, s praznom osnovicom |
| **F4/5** | **Crveni test koji nije naš** — `cascade.authed.spec.js` traži token koji je obrisala ranija cigla. Ili se test popravi, ili se token vrati. | …suita je zelena bez ijedne imenovane iznimke |

**Izlaz iz faze:** `npm run css:debt` pokazuje nulu, `styles.bundle.css` više ne postoji, a
`css:diff` kroz sve rute dokazuje da se ništa nije pomaknulo.

---

### F5 · VJEŽBE

Smjer je zaključan i ne otvara se iznova: *„vježbe su kôd"* je **oboreno mjerenjem** — dvije
trećine su čisti podatak, `params` su deklarirani u svih koje imaju funkciju, a kôd je samo
**formula**. Formula seli u imenovanu, verzioniranu **knjižnicu recepata**.

| cigla | posao | gotovo kad |
|---|---|---|
| **F5/1** | **Prebroji recepte.** Jedina brojka koja odlučuje o cijeni cijele faze još nije izmjerena: koliko različitih recepata pokriva sve postojeće generatore? Mjeri se **prije** obveze. **Ista brojka presuđuje i četiri kvantitativna HR predmeta bez vlasnika** (Leon, 2026-09-06, anketa: *čekaju F5 recepte* — ne radimo ih sami sad, ne padaju). | …postoji broj, pa se tek onda odlučuje ide li se dalje |
| **F5/2** | **Knjižnica recepata + migracija.** Migracija je **samoprovjerljiva**: stari generatori ostaju proročište — isti parametri moraju dati identičan izlaz. | …vježba je 100 % podatak → baza, JSON, `publish_document`, skidanje, MCP i editor je nose bez iznimke, a **BUG-012 se umirovljuje** |
| **F5/3** | **Frontend vježbi.** **Opseg (Leon, 2026-09-06, anketa): SAMO tokeni i razmaci** — engine i tijek ostaju netaknuti, izgled se poravna s ostatkom; prepravak interakcije (unos odgovora, provjera, koraci) NIJE u planu. | …student rješava vježbu koja izgleda i ponaša se kao ostatak platforme |

**Granica koja se ne pomiče:** izgled se smije mijenjati, `generate()` / `answer()` / `type` **ne**.
**Odbačeno i ne vraća se:** evaluator izraza (pokriva manje, a traži vlastiti parser) i sandbox za
korisnički JS (ruši ADR-018 — prava cijena nije sandbox nego to da tuđi kôd odlučuje o ocjeni).

---

### F6 · MCP

Presuđeno je **što** i **kojim oblikom** (ADR-030/031) i **kako se gradi** ([ADR-038](../records/DECISIONS.md#adr-038)):
MCP je **cjevovod** `Learn → kartice → dopune/kviz`, ne skup CRUD-alata. Danas postoji samo read-only pokus izvan
repozitorija. **Plan napisan 17.09.; ①/1 ✅ DOKAZAN OD KRAJA DO KRAJA 18.09.** — Leonov Claude.ai (konektor „Sokrat-Staging", status *connected*) prošao je OAuth i pozvao alat: 40 materijala, police FMTU 1./2. godina i „Moje bilješke". **①/2 brava ✅ · ①/2c brana koja se sama nabraja ✅** (sve STAGING). Grana `feat/f6-mcp`
od `a1d1bb5`, stablo `sokratstudy.f6` (stranica za odobrenje treba F3 mehanizam jezika, „Povezani AI-jevi" profil iz
F2). Sve prvo na STAGINGU.

**Invarijante:** AI je **korisnikov** (mi ne plaćamo tokene → kvalitetu drže brane) · materijal
dolazi kroz chat, datoteku nikad ne vidimo · **sve ide u NACRT** · doseg je **samo vlastito
gradivo**, ni čitanje kataloga · nikad `is_admin()`, nikad `service_role` · **vježbe su izvan MCP-a**.

**🔎 Provjereno 17.09.** Izvori: izvorni kôd `supabase/auth` (master, v2.197.0) · Supabase *OAuth 2.1 Server · MCP
Authentication · Token Security · Deploy MCP servers* · Claude *custom connectors · Authentication for connectors* ·
OpenAI *Developer mode* · upit nad STAGING bazom (samo čitanje).

1. **Supabase Auth = OAuth 2.1 poslužitelj** (beta, bez naplate; AI-korisnik je već naš MAU). **PROD isključen.
   STAGING uključen 17.09.** (Playwright, uz Leonov OK): *Authorization Path* `/odobrenje.html` · DCR uključen ·
   Site URL `http://localhost:5051` (**bio `http://localhost:3000`**, bez Redirect URL-ova — vratiti poslije pokusa).
   Zakucani `supabase-js@2.110.8` ima `auth.oauth` (`getAuthorizationDetails` · `approveAuthorization` ·
   `denyAuthorization` · `listGrants` · `revokeGrant`); ⚠️ `approve`/`deny` **sami preusmjere preglednik** osim uz
   `{ skipBrowserRedirect: true }` — stranica ga uvijek šalje i preusmjerava tek poslije provjere hosta.
2. **CIMD NE postoji** (metapodaci nemaju `client_id_metadata_document_supported`, `client_id` mora biti UUID) → Claude
   bira DCR ili unaprijed registriran klijent. **`resource` (RFC 8707) se prihvaća** i veže na kod, ali token nosi
   `aud: authenticated` → **token nije vezan na naš poslužitelj.** DCR ne ograničava hostove preusmjeravanja (samo shemu).
3. **Token AI-ja = korisnički JWT + `client_id`.** Na STAGINGU izmjereno što takav token danas smije pisati: 7 RPC-ova
   čvorova (i `publish_node`) · `set_profile_handle/identity/image` · tablice `profiles` i `progress` (katalog iza
   admin-RLS-a) · Storage `node-images` i `profile-images` · Edge Functions `delete-account` i `send-notification`.
   ⚠️ **Auth API brava u bazi ne doseže:** `PUT /auth/v1/user` prima OAuth token; lozinka traži ponovnu prijavu tek za
   sesiju stariju od 24 h, osim uz postavku „traži trenutnu lozinku".
4. **Prijavu MCP-a na Edge Functions NE pišemo sami** (ispravak ranije bilješke): `@supabase/server` ≥ 1.6.0
   (`withOAuthProtectedResource` = 401 + `WWW-Authenticate` + metapodaci resursa · `withSupabase({ auth: 'user' })`;
   ugniježđeni oblik je stabilan, `pipeline` je alpha) + `@modelcontextprotocol/server@2.0.0`, uz `verify_jwt = false`.
   Traži asimetrične ključeve — **oba projekta imaju ES256**. Vodič piše `^` → pinamo točno (pravilo #9).
   `check:functions` već ima kalup imenovane iznimke (`mail-unsubscribe`). Iz objavljenog paketa (1.7.0): adresa
   resursa se ZADAJE (`resourceServer`; mi: `MCP_RESOURCE_URL` ili adresa funkcije) · `withSupabase` provjerava samo
   `sub`, **ne `role`** → brava kroz vlastitu ulogu (①/2) ne ruši prijavu u MCP-u.
5. **Klijenti:** Claude custom connector na **Free (jedan) · Pro · Max · Team · Enterprise**, callback
   `https://claude.ai/api/mcp/auth_callback`; DCR registrira novi klijent pri svakom spajanju. ChatGPT developer mode samo
   **Plus · Pro · Business · Enterprise · Edu, samo web**.
6. **Token drži broker Anthropica/OpenAI-ja, ne model** → sigurnost u bazi, kvaliteta u poslužitelju (ADR-038 ④).
7. **ADR-031 pretpostavke koje ne stoje:** ⓐ nacrt osobnog materijala na poslužitelju nije postojao (`publish_document`
   = katalog + `is_admin()`, `SokratDraft` živi u pregledniku, `publish_node` piše živo) → ADR-038 ① · ⓑ „tek nakon
   seobe" — seoba otkazana, OAuth ide na postojeće projekte.
9. **Izmjereno pri gradnji brave (18.09.), oborilo tri pretpostavke plana:** ⓐ odobrenje NE traži preglednik —
   `approveAuthorization` je `POST /auth/v1/oauth/authorizations/<id>/consent`, ali **tek poslije `GET` istog puta**
   (bez njega 404), a veza koja već postoji vrati `400 „no longer pending"` → svaki test traži SVJEŽ DCR klijent.
   Zato brava nije Playwright spec nego mrežna provjera (`npm run mcp:brava`, kalup `delete-account-check.js`).
   ⓑ **`verify_jwt` na Edge Functions provjerava samo POTPIS**, ne `role` → `delete-account` brava u bazi ne doseže
   (izmjereno: AI-token je obrisao račun) → `_shared/token-guard.ts`. ⓒ odbijen RPC, nepostojeći RPC i RPC s krivim
   argumentima daju **isti** odgovor (404 `PGRST202`), a Storage odbijanje dolazi kao **HTTP 400** s `AccessDenied`
   u tijelu → brana koja gleda samo broj bila bi zeleno-slijepa.

8. **Oblik podataka za brane:** kategorija ima obavezan `color` (`KURIRANE_BOJE` u `js/utils.js`) · dopuna se ocjenjuje
   kroz `normFill` (`js/fill-blanks.js`) · `answers` = broj praznina (D2) · shema nema polje veze kartica→pitanje
   (`additionalProperties:false`).

**Cigle** — jedna cigla = jedan commit, gate na svakoj; „crveno" = provjera koja pada na starom kodu.

**① Konektor + OAuth**

| cigla | posao | crveno na starom kodu |
|---|---|---|
| **①/1** okomiti pokus — ✅ STAGING 17.09., **ručno potvrđen 18.09.** | `odobrenje.html` + `js/odobrenje.js` (popis hostova već ovdje) · Edge Function `mcp` v1 s alatom `procitaj_materijale` (jezgra `alati.ts`, samo čitanje) · `npm run mcp:probe`; Leon spaja Claude.ai na `https://czljmvigkgiajzjxtndq.supabase.co/functions/v1/mcp` | `mcp:probe` prije deploya 4/7 palo (404) → poslije 9/9 · `tests/odobrenje.spec.js` 20/20 (①②④ padali: `display:flex` gazio `hidden`, gumbi vidljivi uz odbijen host) · unit `mcp-alati` 11 + `odobrenje` 18, 5/5 mutacija obara test · **ručno POTVRĐENO 18.09.:** Claude izlistao 40 staging materijala (logovi funkcije: 401 → 200, 19:47–19:50) |
| **①/2** brava — ✅ STAGING 18.09. | `supabase/f6-mcp-brava.sql`: uloga `mcp_klijent` (`GRANT … TO authenticator`, `USAGE` na `public`, `SELECT` na `nodes`) + hook `mcp_access_token_hook` (token s `client_id` → ta uloga) + `is_admin()` više nije PUBLIC. Uz to `_shared/token-guard.ts` (`delete-account`, `send-notification` → 403), jer gateway `verify_jwt` **ne gleda `role`**. Brana: `npm run mcp:brava` (mrežna, staging-only) + unit `token-guard` 8 | **crveno izmjereno na starom kodu: 18/24 palo** — AI-token je stvarao čvorove, uzeo `handle`, mijenjao profil, uploadao sliku, prošao admin-vrata `send-notification` i **obrisao račun** (HTTP 200). Poslije brave 24/24; `test:authed` 149/149, `test:rls`/`test:storage`/`test:delete-account` zeleni (hook ide na SVAKU prijavu); 4/4 mutacije obaraju unit |
| **①/2b** lozinka — **polje ✅ 21.09. (`2150156`); postavka OSTAJE** | Polje **Trenutna lozinka** stoji prvo u formi, ima `autocomplete="current-password"` i **nema `minlength`** (zatečena lozinka smije biti kraća od današnjeg praga; sudi joj poslužitelj), a crta se **samo korisniku koji lozinku ima** — onome s Googleom se ne šalje ni kao prazan string. ⚠️ **Pitanje iz plana presuđeno mjerenjem:** `current_password` se u bundleu ne spominje (0 pogodaka), ali `updateUser` **cijeli objekt atributa šalje kao tijelo** — dokazano puštanjem pravog SDK-a s podmetnutim `fetch`-om, uz **kontrolno izmišljeno polje** koje je prošlo jednako (dakle prolaz, ne popis dopuštenih imena). ⚠️ **Drugo mjerenje čini prvi korak bezopasnim:** dok je postavka isključena, poslužitelj polje **tiho ignorira** — i POGREŠNA vrijednost prolazi s HTTP 200. ⚠️ **`imaLozinku()` NIJE provjera „ima li hash"** nego „je li ovo e-mail put": izmjereno je da `identities`/`providers` račun BEZ lozinke **ne razlikuju** od onoga s njom (oba `["email"]`). **✅ POSTAVKA UKLJUČENA na stagingu 21.09. (Leon) i IZMJERENA** (`d604b5e`): bez trenutne lozinke → **400 `current_password_required`** · pogrešna → **400 `current_password_invalid`** · točna → 200 · **oporavak računa mailom → 200, postavka ga NE lomi** (bila glavna bojazan; sonda puštena i PRIJE uključenja kao osnovica) · račun bez lozinke postavlja prvu → 200 · metapodaci → 200. ⚠️ **Poslužitelj za oba slučaja šalje DOSLOVNO ISTU rečenicu**, razlikuje ih samo `error_code` → `authError` sudi **po kodu**; grananje po tekstu ne može razlikovati „nisi upisao" od „upisao si krivo". Prijavljena suita **154/154 s uključenom postavkom**. **✅ CIGLA ZATVORENA** (`c16bcb9`): zastavica `OCEKUJ.authApiZatvoren` je **maknuta, ne prebačena na `true`** — sonda joj je slala **metapodatke**, koje postavka ne dira, pa bi `true` tvrdio da je Auth API zatvoren. Umjesto nje stoji **popis polja** gdje svaki redak nosi točan očekivani odgovor i stvarno se izvede: `password` bez/s krivom `current_password` → **400 `current_password_required`/`current_password_invalid`** (brava) · `data` → 200 i `email` → 400 `email_address_invalid` **ili** 429 (imenovano otvoreno). **Jedna usporedba pokriva oba smjera** — put koji se otvorio pada jednako kao mrtav unos. ⚠️ Očekivanje nosi i **KOD**, jer zatvorena lozinka i otvoren e-mail **oba vraćaju 400** (brana na broj bila bi zeleno-slijepa). Uz to dvije tvrdnje da brava **ne ubije korisnika**: promjena s točnom lozinkom → 200 i **oporavak računa mailom → 200**. ⚠️ **Obrnuta provjera je našla pravi kvar u samoj brani:** prva verzija primala je samo `400 email_address_invalid`, a druga vrtnja unutar prozora vraća **429** → brana bi crvenila bez kvara; oba su ishoda sad imenovana. **40/40** (bilo 35) · preflight 0. ⚠️ **DRUGI KRUG — revizija vratila ciglu, četiri tvrdnje mjerile su ODGOVOR umjesto ISHODA:** „korisnik promijeni lozinku" i „oporavak računa" sudile su **samo HTTP 200** — isti razred koji ova skripta već kodificira i koji je ova cigla već izmjerila (dok je postavka bila isključena, poslužitelj je polje **tiho progutao i vratio 200**), pa bi `{data:{}}` prolazio kao uspjeh → sad se obje dokazuju **prijavom novom lozinkom** · pad pri stvaranju korisnika išao je na `note()`, koji **ne diže brojač** → ukupno 41→38 uz „✅ brava drži" → sad pada zatvoreno + **čegrtaljka na dosegu** · redak `data` svodio se na „status je 200" (prazan zahtjev prolazi jednako) → biljeg se čita **natrag iz odgovora** · sve tvrdnje o porukama sudile su **engleski fallback** (`at()` bez `window.t`), a HR je za našu publiku **zadano** — identična `hr` vrijednost vratila bi kvar uz sve zeleno → sad **nijedne dvije poruke nemaju isti tekst**, ni `en` ni `hr`. Zatvorene i dvije nepokrivene tvrdnje: **U1** račun bez lozinke (Google) postavlja prvu lozinku (drugi način zaključavanja, izmjeren pa nemjeren) i **U2** dvostruka potvrda maila označena kao **NEMJERENA pretpostavka**. ⚠️ Kontrola uz 429 odmah je našla da neispravan token daje **403 `bad_jwt`**, ne 401 — sudi se po odbijanju **po identitetu**, ne po pretpostavljenom broju. Četiri mutacije, sve crvene; kod HR-poruka **`check:i18n` ostaje zelen** (dokaz da tvrdnja treba). Higijena: čišćenje jednokratnih korisnika sad je i na **prekidu** (zatečeno siroče obrisano, 0 preostalih). **42/42** · `auth-error` 35/0 · preflight 0. ⚠️ **Ta sonda mora promijeniti oblik:** danas šalje **metapodatke**, a postavka vrijedi **samo za lozinku** — `PUT /user` s `data:`/`email` ostaje otvoren (izmjereno 200) i mora postati **imenovana** rupa, ne tiho zelena. | unit `lozinka-polje` **24/24** — `changePassword()` se STVARNO izvršava u `vm` sandboxu, jer **polje koje se ne pošalje izgleda identično ispravnom**; authed ③ mjeri **žicu** (tijelo PUT-a nosi `current_password`). **Pet mutacija, svaka crvena na svom mjestu:** maknuto slanje → unit ② **i** authed ③ (ova IMENUJE kvar) · `imaLozinku` uvijek `true` → 3 tvrdnje · obrisan `hr:` → ④ crven, a **`check:i18n` ostaje ZELEN** · krivi `autocomplete` → crven · polje maknuto iz forme → 4 tvrdnje. ⚠️ Šesta mutacija se **nije primijenila** (2 pogotka — i komentar), skripta ju je odbila i to ispisala: brojač pogodaka spriječio je lažno crveno. `test:unit` **554/0** · preflight **EXIT 0** · prijavljena suita **154/154**. ⚠️ **DRUGI KRUG — `brana-revizor` VRATIO ciglu i bio u pravu:** jezgra (žica) je držala, ali **pet tvrdnji o OBLIKU FORME mjerilo je tekst datoteke, ne iscrtano polje**. Najteže: „polje je uvjetovano `imaLozinku(user)`" bio je regex koji **zadovoljava sama deklaracija funkcije** — pa su dvije jednoredne mutacije prolazile **24/24 zeleno**: `(true` (Google dobiva polje koje ne može ispuniti, `required` ga **zaključa**) i `(false` (**polje ne dobiva nitko** → s uključenom postavkom nitko ne mijenja lozinku). Popravak je jedan i zatvara četiri nalaza: brana **stvarno iscrta profil** (dvaput — e-mail i Google) i sudi nad `innerHTML`-om; lažni DOM **više ne izmišlja elemente** (inače bi „Trenutnu lozinku" vratio i ondje gdje je nema); sudi se nad **jednim `<input>`-om** umjesto nad prozorom od ±200 B (stari je imao **178 B rezerve** do istog atributa u komentaru); negativna tvrdnja o `minlength` **više ne prolazi na nuli**; fokus se mora **dogoditi**, ne postojati kao tekst; dodani `required` i `type="password"`. Šest novih mutacija, sve crvene i sve imenuju (`(false` → **8 crvenih**), jedna se nije primijenila (3 pogotka) i brojač ju je odbio. **33/33** (bilo 24) · `test:unit` **563/0** · preflight **0**. **Nepopravljeno svjesno** (cigla ostaje jedna): N3 popis mjesta koja mijenjaju lozinku (`js/auth.js:865` = oporavak) · N1 „oba jezika" samo za `profile.*Pass*` (izmjereno: 655 ključeva, 0 bez `hr` → obrat je besplatan) · **S1 wire-tvrdnja se u CI-ju PRESKAČE** (traži `STAGING_*`, job ne blokira merge) · N5 tvrdnja koju ①/2c mora **okrenuti** |
| **①/2c** brana koja se sama nabraja — ①/2c‑1 ✅ STAGING 18.09. | popis ZABRANJENOG → popis OTVORENOG. `supabase/f6-mcp-inventar.sql`: `mcp_brava_inventar()` (`security definer`, `execute` samo `service_role`) nabroji sheme, tablice, funkcije i buckete iz kataloga baze; `OTVORENO` u brani drži ono malo što smije (danas: shema `public`, `select` na `nodes`, **nijedna funkcija**). Uz to: izuzeće `/^mcp_/` suženo na točno ime hooka · `_node_own` i `mcp_brava_inventar` ušli u žive pozive · iscrpna REST-proba čitanja SVAKE tablice izvan popisa (⚠️ 200 s praznim `[]` NIJE odbijanje — znači da dozvola postoji). **①/2c‑2 ✅ STAGING 18.09.:** svaka Edge Function s diska mora biti pod stražom (`_shared/token-guard.ts`) ILI imenovana s razlogom (`mail-unsubscribe` = HMAC iz linka, `mcp` = radi pod korisnikovim RLS-om), svaka pod stražom mora imati i živu provjeru, a upload se probija u SVAKI bucket iz inventara (3), ne samo u `node-images`. | obrnuta provjera na stagingu: tri privremene dozvole (`select` na `subject_content`, `usage` na `graphql_public`, `execute` na `is_admin()`) → **5 padova, svaki imenuje svoje**, uključujući čitanje javnog KATALOGA kroz PostgREST (HTTP 200 s gradivom) — invarijanta ADR-031 „ni čitanje kataloga" do sada nije bila izmjerena. Lažni `mcp_zapocni_nacrt(...)` u `supabase/*.sql` → inventar crven. Za ①/2c‑2 tri mutacije: nova funkcija bez straže → crveno · ista pod stražom a bez žive provjere → crveno · mrtav redak u popisu → crveno (izvor vraćen bajt-identično, sha256 prije/poslije). Poslije povrata **35/35** (bilo 24) |
| **①/3** stranica za odobrenje — ✅ 20.09. | Ime klijenta, host povratka, popis dopuštenih hostova i HR/EN **stigli su već s ①/1** (provjereno u kodu prije rada). Ostajala je **prijava uz očuvan `authorization_id`**, i to je ova cigla: poveznica na prijavu vodila je S te stranice, pa je s njom nestajao i `authorization_id` — točno kvar iz ručnog pokusa 18.09. Sad prijava ide u **DRUGI prozor** (`cekajPrijavu` u `js/odobrenje.js`), stranica ostaje na svojoj adresi i sama nastavi; vidi je `storage` (događaj koji preglednik šalje samo DRUGIM dokumentima istog origina), druga mreža je `visibilitychange`, zaustavljen prozor se kaže naglas. Prijava ostaje ona prava, s Googleom — `js/auth.js` i naslovnica se NE diraju. **Telefon je izdvojen u ①/3b** (Leon, 20.09.: „stavi kao zasebne cigle"). | **Izmjereno prije koda** (sonda na stagingu, nije ostala u repou): detalji se PRIJE prijave ne mogu ni pokazati (`GET /oauth/authorizations/<id>` bez tokena → **401**), ali nedovršeno povezivanje obilazak **preživi** — u 0 s / 61 s / 181 s još „pending", odobrenje nakon 181 s prolazi (200). Brana ⑦ u `tests/odobrenje.spec.js`: bez osvježavanja i bez novog povezivanja stranica iz „prijavi se" prijeđe u „Dopusti / Odbij", a `authorization_id` je i dalje u adresi. **36/36** (9 tvrdnji × 4 profila) · unit odobrenje 18/18 · preflight EXIT 0. **Agent `brana-revizor` vratio je ciglu s četiri nalaza** (`bdc4e2b`), sva četiri potvrđena u kodu: druga mreža (`visibilitychange`) bila NEMJERENA · zaustavljen `window.open` bez tvrdnje · `unit:52` se zvala „oba preusmjeravanja" a brojala samo `location.assign` dok je cigla uvela prvi `window.open` · en+hr se brojao RUKOM za jedan ključ → sad se nabraja iz izvora (18 ključeva). ⚠️ **Dokaz da zadnje treba:** s obrisanim `hr:` **`check:i18n` ostaje zelen** (gleda postoji li ključ, ne oba jezika). Pet obrnutih provjera, svaka crvena na svom mjestu, izvor svaki put vraćen bajt-identično; `location.assign('/')` → ⑦ pada u **7 s** umjesto 120 s isteka, jer tvrdnja o adresi stoji PRIJE čekanja na prozor |
| **①/3b** telefon za stranicu odobrenja — ✅ 21.09. | `odobrenje.html` se sad mjeri u **oba stanja** („prijavi se" i „Dopusti/Odbij") na sve četiri širine → **60 ekrana umjesto 52**. ⚠️ **Mjerenje je oborilo pretpostavku SAME BRANE:** naivno dodana, stranica je dala **14 nalaza koji na pravom iPhoneu ne postoje** (logo i prekidač jezika „ispod Dynamic Islanda"). Tvrdnje ①/⑥/⑦/⑦b vrijede samo za stranicu koja se **`viewport-fit=cover`-om izričito prijavila** da crta ispod izreza; bez te prijave iOS je slaže UNUTAR sigurne zone. Premisa je dotad bila nevidljiva jer je svaka mjerena stranica imala `cover`. Sad se **čita sa stranice** (`podIzrezom` u `mjeri()`), a `BEZ_IZREZA` imenuje tko je smije nemati i zašto. Nova tvrdnja **⑩** čuva premisu s obje strane. ⚠️ Obilazak (`EKRANI_ODOBRENJE`) je **namjerno odvojen** od premise (`BEZ_IZREZA`) — prva verzija ih je spojila, pa bi buduća stranica bez `cover`-a koja nije dio obilaska srušila ⓪ bez veze s kvarom. | **Tri obrnute provjere, sve crvene, izvor svaki put vraćen:** ① stranica DOBIJE `cover` → vrati se svih 14 nalaza **i** ⑩ javi mrtav unos (dokaz da premisa, a ne slučaj, drži nalaze ugašenima) · ② unos maknut iz `BEZ_IZREZA` → ⑩ imenuje sve 4 širine, a ⓪ **ostane zelen** (dokaz da je razdvajanje popisa radilo) · ③ `index.html` izgubi `cover` → ⑩ imenuje svaki ekran aplikacije (rupa zatvorena). 12/12 na suiti, preflight EXIT 0. **Drugi krug, poslije revizije brane** (`42298d7` · `56a4dfc`), dva nalaza izvan jezgre, oba potvrđena u kodu: ⓐ **② KROMO je i dalje pretpostavljao izrez** — gašenje je bilo napisano kao četiri posebne iznimke, pa je budžet trake ostao računati `vh − 59` i na stranici koja izrez nikad ne dobije, dakle **popust**. Sad premisa teče kroz **jednu** varijablu (bez `cover`-a je rub sav na nuli; pojasevi su ionako čuvani s `if (R.x > 0)`), pa se ①/⑥/⑦/⑦b gase sami, ② prestaje davati popust, a `postaviRub` prestaje ubrizgavati 59/34 px kojih ondje nema. **Izmjereno mutacijom** (ljepljiva traka od 120 px): novi kod prijavi **21 % na 320 px**, stari **ništa** (12 % nakon popusta) — u landscapeu su oba crvena jer ondje popusta nema. ⓑ **`editor.html` je bio jedini dokument s `cover`-om koji ne gleda nijedna ⑩** (mjeri ga samo prijavljena suita) → i ona je dobila svoju ⑩; obrnuto: `editor.html` bez `cover`-a → ⑩ imenuje `editor` i `admin` na sve 4 širine. Usput: premisa se čita `querySelectorAll(...).some(...)` (s `querySelector` bi drugi `<meta>` s `cover`-om dao lažno „nema"), a poruka ② uzima nazivnik iz mjere (prije je pisala „21 % (120 od 509 px)" — dva broja koja se poriču). Javna 12/12 · prijavljena 13/13 · preflight 0 |
| **①/3c** pet samostalnih stranica bez ijedne mjere — ✅ 21.09. | Nalaz revizije ①/3b: root ima 8 `*.html`, a **`contact` · `faq` · `odjava` · `privacy` · `terms` nisu imale nikakvu mjeru za telefon** — a do njih vodi podnožje svake druge stranice. Sad su u obilasku: **80 ekrana umjesto 60** (`odjava` se mjeri BEZ tokena — stanje u kojem je pravi posjetitelj i zatekne). Nova tvrdnja **⑪** veže popis za **DISK**: svaki `.html` u korijenu je ili obiđen ili imenovan uz mjesto gdje se mjeri (`editor.html` → prijavljena suita); nova stranica i mrtav unos padaju po defaultu. Pet stranica ulazi u `BEZ_IZREZA` jer danas nemaju `cover` — **poravnanje ih prazni** (sljedeća cigla). | Obrnuto: nova `.html` u korijenu → ⑪ je imenuje · mrtav unos → ⑪ pada. ⚠️ **Obrnuta provjera oborila je moju zakrpu DVAPUT**, i to je glavni nalaz cigle: izbacivanje stranice iz obilaska **nije** oborilo ⓪, jer ⓪ očekivani broj računa iz ISTOG popisa iz kojeg hoda (tvrdnja o sebi); uhvatila ga je samo ⑩, i to slučajno — ta mreža nestaje čim stranice dobiju `cover`. Prva zakrpa je očekivanje gradila iz `G.SAMOSTALNE` i bila **jednako samoreferentna** (opet zeleno). Tek kad očekivani ekrani stoje **uz disk** (`DOKUMENTI[...].ekrani`), izbacivanje pada i imenuje stranicu. Javna 13/13 · prijavljena 13/13 · preflight 0 |
| **①/3d** poravnanje samostalnih stranica — ✅ 21.09. (Leon: *„naravno da treba sve poravnati"*) | Šest samostalnih dokumenata (odobrenje + 5 pravnih) bilo je na iPhoneu **uokvireno** — traka pozadine gore i dolje umjesto boje od ruba do ruba. Sad se svi prijavljuju `viewport-fit=cover`-om, a `css/legal.css` nadoknađuje izrez i home-indikator kroz `var(--safe-*)` (nikad `env()` — jedan izvor je `css/variables.css`, brana `check:safearea`). Podloga trake ide do ruba; pomiče se samo ono što se tapka i čita. **`BEZ_IZREZA` je time PRAZAN** — i to je ishod, ne propust: na svakoj mjerenoj stranici pravila o sigurnoj zoni sad se stvarno primjenjuju. | ⚠️ **Usput popravljena rupa u MJERI, bez koje bi cigla bila lažna:** petlje za samostalne dokumente (①/3b i ①/3c) nikad nisu zvale `postaviRub`, pa je stranica imala sigurnu zonu od **0 px** dok ju je `mjeri()` sudio po profilu od 59 — nadoknada se doslovno nije mogla vidjeti, a prvi prolaz poslije popravka CSS-a vratio je **bajt-identične koordinate**. Te bi stranice ostale crvene što god napisali. Obrnuto (poslije popravka): nadoknada maknuta iz telefonskog pravila → ① imenuje logo i prekidač · maknuta iz osnovnog → ⑦ imenuje bočni pojas u landscapeu. Javna 13/13 · odobrenje 36/36 · prijavljena 13/13 · preflight 0 |
| **①/3e** bočni pojas se mjeri i na vrhu — ✅ 21.09. | Nađeno dok sam provjeravao ①/3d: mutacija koja zaglavlju oduzme bočnu nadoknadu oborila je **samo dvije od šest** stranica, a razlika nije bila u kvaru nego u **duljini teksta**. ⑦ se dotad mjerio isključivo u fazi „rubovi", koja stranicu prvo spusti do dna (to treba ⑥) — a stranica koja se skrola dotad odnese zaglavlje s ekrana: izmjereno, pravna stranica odskrola **3 461 px** i logo završi na `y = −3442`. Isto vrijedi za svaki ekran aplikacije koji se skrola. Bočni pojas tu ogradu **nema** (nema vodoravnog skrola), pa se sad mjeri i **odmah**, prije spuštanja; nalazi obje faze se zbrajaju, isti element u obje = jedan nalaz. | ista mutacija sad obori **svih šest** stranica umjesto dvije. Bez mutacije: javna 13/13 · prijavljena 13/13 · preflight 0 — dakle proširena provjera ne otkriva **nijedan** novi kvar, stranice su stvarno čiste |
| **①/4** ugradnja | imenovana iznimka u `check:functions` · točno pinani paketi · **`www.sokratstudy.com/mcp` kroz Vercel rewrite** (provjera na previewu) | `check:functions` crven bez iznimke |
| **①/5** Povezani AI-jevi — ✅ 20.09. | u profilu kartica „Povezani AI-jevi": popis veza (ime, datum) + **Prekini vezu** (`listGrants` / `revokeGrant`), HR/EN, CSS, 4 tvrdnje u `tests/ai-veze.authed.spec.js`. ⚠️ **IZMJERENO 20.09.:** prekid ubija OBNOVU (`refresh` → 400 odmah), ali već izdana propusnica vrijedi **3600 s** → tekst u kartici to KAŽE (trajna napomena, ne toast). Leon presudio: istina sad, trenutan prekid je zasebno pitanje (tražio bi provjeru na svakom čitanju svakog korisnika = vrući put). | ime aplikacije je TUĐI tekst (DCR je otvoren): bez `escapeHtmlProfile` se `<img onerror>` IZVRŠI — mutacija oborila spec ②; bez trajne napomene pada ① |

Rizici: beta poslužitelj, mlad `@supabase/server` · **greška u hooku = nitko se ne prijavi** (hook ide na svako izdavanje
tokena) → nekoliko redaka, prvo staging, izlaz = isključiti hook u dashboardu · `PUT /user` ovisi o postavci ·
ograničeno CPU vrijeme Edge Functiona · besplatni Claude = jedan konektor · rewrite za MCP promet neprovjeren.

**② Cjevovod u nacrt**

| cigla | posao | crveno na starom kodu |
|---|---|---|
| **②/1** nacrt | tablica `node_drafts` + `mcp_*` RPC-ovi · kvota (nacrti u izradi, veličina) | tuđi nacrt nevidljiv · kvota · živi `node_content` bajt-isti nakon svih MCP poziva |
| **②/2** alati | `procitaj_materijale` → `zapocni_nacrt` (lekcije s bojom) → `napisi_learn` → `dodaj_kartice` (pada bez Learna) → `dodaj_pitanja` (pada bez kartice) → `predaj_nacrt` · `procitaj_nacrt`; upute cjevovoda u `instructions` poslužitelja | unit nad modulom alata + e2e: Node MCP klijent s pravim tokenom prođe cjevovod na stagingu |
| **②/3** oblik materijala | nacrt → payload (kategorija po lekciji, v2 id-evi, bez slika i videa u prvom izdanju); veza kartica→pitanje = opcionalno polje `card` u shemi (aditivno) | `validate:schema` nad izlazom |
| **②/4** pregled i prihvat | „Nacrti od AI-ja" u Mojim materijalima, isti renderer · **Prihvati** = jedna transakcija, samo obična sesija · **Odbaci** | nacrt → prihvat → materijal se uči; OAuth token ne može prihvatiti |
| **②/5** ulaz | „Spoji svoj AI" (ADR-026: jedna radnja, dva ulaza) → upute + kopiraj URL; lažni tekst `studio.js:235` nestaje | spec |

Rizici: 200 kartica u jednom pozivu → alati po lekciji · AI preskoči korak → redoslijed u potpisu alata · prompt
injection iz PDF-a → doseg je vlastiti nacrt (ADR-026).

**③ Četiri brane** — u MCP poslužitelju, iz istih modula koje čita preglednik, i ponovno pri **Prihvati**.

| cigla | posao | crveno na starom kodu |
|---|---|---|
| **③/0** mjerenje | katalog: duljina odgovora dopune, nalazi li se odgovor u kartici → prag brane ④ | — (brojke se upisuju ovdje) |
| **③/1** jedan izvor | `card-limits.js`, paleta i `normFill` u Deno-u: uvoz izvan `supabase/functions/` ili generirana kopija s drift-branom (kalup `build:css`); `KURIRANE_BOJE` i `normFill` u male module po kalupu `card-limits.js` | drift-brana pada na razlici |
| **③/2** duljina | 501 odbija s mjestom; 201–500 prolazi uz upozorenje AI-ju | 500 prolazi, 501 pada |
| **③/3** pitanje po kartici | `predaj_nacrt` pada ako kartica nema ni kviz ni dopunu | unit + e2e |
| **③/4** boja | svaka lekcija ima boju iz kurirane palete | unit |
| **③/5** jednoznačna dopuna | praznine = odgovori · neprazan poslije `normFill` · bez HTML-a i LaTeX-a (razred BUG-024/025) · kratak (prag iz ③/0) · nalazi se u izvornoj kartici · nije već napisan u rečenici | unit po obliku (danas prolazi 2 praznine uz 1 odgovor) |
| **③/6** druga linija | iste brane pri **Prihvati** | nacrt ubačen izravnim RPC-om s karticom od 501 znaka → prihvat odbija |

Rizik: prestrog prag ④ → AI zapne; zato ③/0 ide prvo.

**PROD — zadnje, svaki korak uz Leonov izričit OK:** SQL u SQL Editoru → OAuth poslužitelj + hook u dashboardu →
funkcija → klijent.

**Leon, druga anketa 17.09.:** radi se u stablu `.f6` · dashboard kroz Playwright prozor · pokus na njegovom
Claude.ai računu · **ChatGPT Plus ima → testira se i on** (prije PROD-a) · pomoćni subagenti u radu na F6 (mehanički
posao i napadač na brave) · lozinka = cigla ①/2b · dozvola za push `feat/*` upisana.

**✅ ①/1 ZATVOREN 18.09.** Prošao je tek iz trećeg pokusa, i dva pada su dala nalaze koji ostaju kao pravila:
ⓐ **zadani preglednik otvara stranicu odobrenja** — Leon je radio u Braveu, a zadani je bio Edge, pa prebacivanje
na staging ondje nije postojalo (→ cijeli tok u JEDNOM pregledniku, i taj mora biti zadani);
ⓑ stranica se na `localhost`-u tiho vraćala na produkciju → zakrpano (`oauth.noProject`, spec ⑥).
Dijagnoza je išla kroz `query_logs` (`edge_logs` za OAuth, **`function_edge_logs` za pozive funkcije** — u
`edge_logs` ih NEMA, što je prvo zavelo na krivi trag).

**(povijest) Čekalo Leona za kraj ①/1:** u pregledniku na `http://localhost:5051` prijava na STAGING (override, račun
`test-admin@sokrat.local` — jedini staging račun s gradivom: 5 polica, 40 materijala) → Claude.ai →
*Customize → Connectors → Add custom connector* → staging adresa iz ①/1 → „Dopusti" → pitati Claude za materijale.
**①/2 dashboard korak je IZVEDEN** (Leonov OK, 18.09.): *Auth Hooks → Customize Access Token (JWT) Claims* →
Postgres → `public.mcp_access_token_hook` → Enabled, na STAGINGU. Izlaz u nuždi je isti taj zaslon.

**Nalaz prve vrtnje inventara (①/2c‑1):** `mcp_klijent` je smio izvršiti `set_updated_at()` i `touch_subject_content()` — jedine dvije trigger-funkcije bez izričitog `revoke` (ostalih osam ga ima), pa su zadržale PUBLIC EXECUTE koji Postgres daje po defaultu. Nisu bile ruta (vraćaju `trigger`), ali su bile dozvola bez razloga → zatvorene, i popis otvorenih funkcija je time PRAZAN. Okidači rade dalje (EXECUTE se provjerava pri stvaranju triggera, ne pri upisu; `test:authed` zelen).

**Što ①/2 nije zatvorio, a ①/2b jest — i dokle:** `PUT /auth/v1/user` ide na Auth API mimo Postgresa, pa ga brava u
bazi ne doseže. **Zatvorila ga je postavka „Require current password" (staging, 21.09.), ali SAMO za lozinku.**
`data: {…}` i `email` prolaze i dalje. Zastavice `OCEKUJ.authApiZatvoren` **više nema** — zamijenio ju je popis
`AUTH_POLJA` u `scripts/mcp-brava-check.js`, gdje svaki redak nosi točan očekivani odgovor i stvarno se izvede;
metapodaci i e-mail ondje stoje kao **imenovano otvoreni**, kao `mail-unsubscribe` u `check:functions`.
„Require reauthentication" je **druga** postavka (nonce mailom) i nije naš put.
⚠️ **NEMJERENO, zapisano kao pretpostavka:** da promjenu maila čuva dvostruka potvrda (`Secure email change`) —
mjerenje bi tražilo slanje pravog maila. Ako ta potvrda nije uključena, put „AI promijeni mail → zatraži reset →
preuzme račun" stoji otvoren, a brana bi ostala zelena jer je redak otvoren **po planu**.
⚠️ **Na PRODUKCIJI postavku ne mjeri nitko** — brana tvrdo odbija prod. Danas nije rupa (MCP živi samo na stagingu),
ali u **F7 objavi** uključenje postavke mora dobiti imenovan korak.

---

### F7 · OBJAVA

| cigla | posao | gotovo kad |
|---|---|---|
| **F7/1** | **Dijeljenje** — link s tajnim tokenom, **bez javne biblioteke** u prvoj fazi (presuđeno unaprijed). | …autor pošalje link i primatelj uči iz njegova materijala |
| **F7/2** | **Povijest učenja** — plitka izvedba, onako kako je Leon presudio: vrijednost joj raste s količinom materijala, pa je pravo vrijeme tek sada. | …vidi kad je zadnji put učio što |
| **F7/3** | **Kretanje kroz vrijeme na ekranu napretka** — grafikoni. Analitika o karticama se **ne** proširuje (Leonov sud). | …vidi kretanje kroz vrijeme, ne samo trenutni zbroj |

**Smjer objave (Leon, 16.09. — smjer, oblik NIJE presuđen, cigle ne postoje):** kad dijeljenje stigne, **sav
katalog ide pod Leonov profil**, a korisnik na ulazu **vidi sve objavljene predmete i sadržaje**. Dok nema
korisnika, **AI profili** objavljuju zanimljive stvari (*„to je samo za početak"*); objavit će se **cijela
1. godina FMTU na hrvatskom**, a botovi mogu objavljivati s **pravnog fakulteta** (Leon ima radove studenata
i skripte). Moj prijedlog uz to (bez Leonove presude): katalog najprije samo PRIKAZATI pod profilom, a selidbu
u materijale tek poslije F5 (vježbe su kôd, ADR-024 otoci) · javni pregled je nova cigla iza F7/1 (koja je
namjerno „bez javne biblioteke") · AI profili jasno označeni kao AI, sadržaj kroz agente bez API ključa i
kroz iste brane kao MCP, bez sudjelovanja u natjecanju (Uvjeti v2 zabranjuju namještanje rezultata).

---

## 4 · Stalna traka — sitni dug

Ne čini fazu i ne čeka svoj red; puni praznine kad cigla završi ranije. Ništa odavde ne blokira
ništa, ali ništa ni ne nestaje samo od sebe.

- **Znak i fontovi nikad nisu izmjereni.** `assets/logo.svg` je 45 KB za znak od 32 px, a
  `check:budget` mjeri **samo skripte** — fontovi mu nikad nisu bili u vidnom polju. Nakon što je
  učitavanje po ruti spustilo skripte, oni su vjerojatno najveći preostali teret prvog kadra.
- **Zaštita prijave od nasilnog pogađanja** (rate-limiting na Supabase Authu).
- **Nježna uputa pri prijavi slabom lozinkom** — `data.weakPassword` se danas svjesno ignorira.
- **Sidebar predmeta** — `openSidebar()` nema nijednog pozivatelja. **Odluka (Leon, 2026-09-06,
  anketa): OBRISATI** — `openSidebar()`, `sidebar.css`, markup i i18n ključevi idu van u F4 (uz `check:orphan-css` osnovicu).
- **Baza i Storage:** siročad u Storageu · staging poravnati sa `supabase/f1-nodes.sql` ·
  `set_updated_at` ima promjenjiv `search_path` (jedini nenamjeran sigurnosni WARN).
  ⚠️ `is_admin()` se **ne smije** revokeati `authenticated`-u — zovu ga RLS politike kao pozivatelja.
- **HR u Supabase** kad HR program bude potpun. Uz to: `management-hr` datoteke nemaju id-jeve →
  `add-item-ids.js` prije nego HR podrži item-ops.
- **Editorski polish** (neobavezno, `archive/EDITOR_PLAN.md` §12): resize + callout · boje sekcija ·
  paste tablice iz Excela · zadnji vizualni prolaz · upload · chart · povijest verzija i „Vrati" ·
  `final` kao kompozicija umjesto kopije · `schemaVersion` u runtimeu.

---

## 5 · Što svjesno NIJE u rasporedu

Zapisano da se ne otvara iznova, ne da se planira.

- **Matura** — Leon: *„neću otvarat maturu."* Blokator je nepotvrđeno pravno pitanje o objavi
  NCVVO materijala. **Vizija** (što bi bila i kad bi se otvorila): [VISION.md](../product/VISION.md) §8 (Leon, 2026-09-05: *„to je vizija"*).
- **Simulacija vođenja hotela** — zaseban proizvod ([ideas/HOTEL_SIM.md](../ideas/HOTEL_SIM.md)),
  posuđuje naše primitive ali nije Sokratova značajka.
- **Spaced repetition** — nije otkazan, samo više nije sljedeći.
- **SEO / prave adrese umjesto hash-ruta** — arhitektonska odluka, ne cigla. Nagrada je manja nego
  zvuči: dijeljenje ide tajnim linkom, pa korisničko gradivo po dizajnu nije javno pronalažljivo.
- **Monetizacija** — Faza 4, tek na skali. Naplaćuje se **funkcionalnost, ne sadržaj**.
- **HR content-staza sa suradnikom** — suradnja otkazana 2026-09-04. Gradivo ostaje; četiri
  kvantitativna HR predmeta su **bez vlasnika**, ne otkazana.
- **Seoba sa Supabasea** — otkazana; Pro se plaća do daljnjeg.

---

## 6 · Što čeka Leonovu riječ

Osam pitanja koja mijenjaju izvedbu, a ne mogu se razumno pretpostaviti — **svih osam odgovoreno** (zadnjih pet anketom, 2026-09-06); ostaju kao zapis zašto je što odlučeno:

1. ~~**Neprijavljen korisnik na tuđem uređaju** — zadnja lokalna tema ili zadana?~~ **Odgovoreno 2026-09-06: zadana** — odjava briše lokalni izbor, *„tuđi izbor ne smije preživjeti odjavu"*. *(F2/1)*
2. ~~**Sidebar predmeta** — obrisati ili vratiti kao brzi izbornik?~~ **Odgovoreno 2026-09-06 (anketa): OBRISATI** — landing i browse vode do svakog predmeta; `openSidebar()`, `sidebar.css` i markup odlaze u F4 čišćenje. *(F4)*
3. ~~**Opseg frontenda vježbi** — tokeni i razmaci, ili prepravak interakcije?~~ **Odgovoreno 2026-09-06 (anketa): SAMO tokeni i razmaci** — engine i tijek ostaju, izgled se poravna (boje, razmaci, kadar na telefonu). *(F5/3)*
4. ~~**Facebook prijava** — čeka Metine ključeve; kod se vraća jednom zastavicom.~~ **Odgovoreno 2026-09-06 (anketa): ODUSTATI za sad** — Google pokriva većinu; ostatak koda iza zastavice se briše u F2, vraća se jednom zastavicom ako ikad zatreba. *(F2)*
5. ~~**Četiri kvantitativna HR predmeta** — radimo ih mi, ili padaju?~~ **Odgovoreno 2026-09-06 (anketa): ČEKAJU F5 recepte** — prebrojavanje recepata (F5/1) pokaže mogu li im vježbe biti čisti podatak; tek onda se zna cijena. Ne radimo ih sami sad, ne padaju. *(F5/1)*
6. ~~**Tinder-špil kartica** — zamjenjuje današnji prikaz na telefonu, ili je prekidač uz njega? Nosi li smjer povlačenja značenje (desno = znam)?~~ **Riješeno 2026-09-06 (F1/9 isporučen):** Leon — *„samo na mobitelu"*; gesta je DODATAK uz gumbe, pa prekidač nije potreban; desno = znam. Ostaje samo presuda palcem na iPhoneu. *(F1/9)* — ~~I za F1/8: koji preglednik na telefonu vidi ljepljivi hover~~ **otpalo 2026-09-05:** na iPhoneu svaki preglednik vrti WebKit, a WebKit s dodirom kvar reproducira (izmjereno, §F1/8).
7. **„Automatic · Carbon" u biraču tema** (Leon, 2026-09-06: *„glupo, ne kužim smisao"*) — gumb postoji jer je Leon tražio da stranica bez izbora prati uređaj *„kao mail"* (F1/3); sufiks „· Carbon" samo kaže što uređaj sad bira. Tri izlaza: **(a) natpis samo „Automatski"** (opis ispod već kaže da prati uređaj) · (b) gumb maknuti — ali tada se jednom napravljen izbor **ne može poništiti**, uređaj se više nikad ne prati · (c) zadržati sufiks, ali kao podnaslov, ne kao ime. **Odgovoreno 2026-09-06 (anketa): (a)** — gumb ostaje, sufiks otpada. **Isporučeno 2026-09-06** (`feat/nocna-b`): `themeCardHtml()` više ne čita temu uređaja, natpis je točno „Automatski" na oba jezika i na svakom uređaju; što gumb radi kaže opis iznad birača (`profile.appearanceDesc`) — jedno mjesto, ne dva. Brana: `tests/unit/theme-picker-label.test.js` (18 tvrdnji; obrnuto kroz staro stablo = 11 crvenih). *(F2/1)*
8. ~~**Kraj špila** — zadnja kartica + palac desno (F1/13): samo odskok, ili ponuda „ponovi ne-znam" / „ispočetka"? I: trebaju li ✓ / ✕ tipkovničke prečace na stolnom?~~ **Odgovoreno 2026-09-06:** *„Kada ode desno do zadnje kartice bude izbornik: ispočetka, promiješaj, ponovi ne-znam. Na kompu strelica desno, ✕ lijevo; sve se može tipkama."* → F1/13 (izbornik + X / Z), F1/12 (red gumba), F1/14 (tutorial, kasnije). *(F1/13)*

---

*Mjere, nalazi i obrazloženja: [BACKLOG.md](../records/BACKLOG.md) · odluke:
[DECISIONS.md](../records/DECISIONS.md) · što je isporučeno:
[CHANGELOG.md](../records/CHANGELOG.md) · zašto brana postoji: zaglavlje njezine skripte.*
