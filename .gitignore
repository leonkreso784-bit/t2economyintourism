# Dependencies
node_modules/
# package-lock.json se VERZIONIRA (reproducibilan `npm ci` u CI-u, FOUNDATION_PLAN F1).

# Lighthouse CI radni izlaz (rezultati se ne commitaju; budžeti su u .lighthouserc.json)
.lighthouseci/

# IDE
.vscode/
.idea/
*.swp

# Obsidian (vault config je strojno-specifičan; osobni vault folder nije dio projekta)
.obsidian/
Sokrat study/
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
npm-debug.log*

# Build outputs
dist/
build/
.cache/

# Playwright (visual responsiveness tests)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
# Spremljena admin-sesija za authenticated suite (storageState) — NIKAD se ne commita
/tests/.auth/

# Environment
.env
.env.local
.env.*.local

# Vercel
.vercel

# DB backupi (risk-sprint #3) — sadrže KORISNIČKE podatke (profiles/progress); NIKAD u git
backups/

# Source materials (professor PPT/PDF/JPG) — copyright, never commit
_materials/

# Dokazni screenshotovi (ad-hoc, izvan gita)
_screenshots/

# Scratch / staging dirs (generator + PDF→img + ad-hoc) — all temporary
tmp/
tmp-*/

# Python / dev artifacts (legacy PDF-extraction path; replaced by scripts/pdf-text.js)
.venv/

# Osobne strojno-lokalne Claude bilješke (TEAM.md §1)
CLAUDE.local.md
